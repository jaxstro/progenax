# progenax — status

next: **Provenance & credibility audit (release-prep) — Phase-1 inventory + Batch-1 DONE (2026-06-14, branch `feat/provenance-credibility-audit` off main).** Design `docs/plans/2026-06-14-provenance-credibility-audit-design.md` (D1–D4 locked). **Phase-1 inventory:** 17 read-only agents → **270 findings (HIGH 67 [released 41/docs 15/exp 11], MED 137, LOW 66; 37 needs-fetch)**; triage verdict **NO SYSTEMIC ROT → triage-first** (classic fingerprints mostly already-fixed; HIGH concentrated in 3 buckets — A live phantom-API docs, B held-PDF verify, C cited-but-not-held). Reusable ledger committed `docs/provenance-ledger.md` (091bf94); full inventory `.claude-work/provenance-inventory-2026-06-14.md`. **CHECKPOINT 1 (Anna):** fetched Tier 1+2 PDFs (Demircan&Kahraman1991, Standish/IAU2009, Zahn1977, Hurley2000/2002, Lucy2006, King1962, Aarseth1974); chose **rewrite phantom-API docs to the real BirthEnvironment/build_spatial_ic API**. Caught + corrected a workflow false-positive: `Säilynoja_2026.pdf`/`Prša_2016.pdf` ARE held (accented filenames ASCII-stripped by the canonical list). **Batch 1 (Marks/Jerabkova α₃) DONE (661fe08):** read the held Marks 2014 erratum PDF → confirmed FP threshold **x'≥−0.87** (Eq.1 corrects Marks+2012 Eq.14 p.2252); operative code (MARKS/JERABKOVA_COEFFICIENTS, both −0.87) already correct + Table 3 matches PDF cell-for-cell; removed the stale published-typo **+0.87** surviving in `environment.md` (a theory-doc warning literally asserted +0.87 as *correct*), `marks-2012.md` (Notes mischaracterized the erratum), `differentiability.md`; added `Marks2014` erratum bibkey. `make build` 161 pages, **0 warnings**; src/ untouched → gate unaffected. **Batch 2 (Moe Table 13) DONE (9235f66):** verified all 80 code cells exact vs PDF p.52; Moe "Table 10" γ(M₁) cite was WRONG (Table 10 p.27 = VB-eccentricity-η table) → corrected to "period-averaged reduction of Table 13" across binary.md/multiplicity-statistics.md/mass-ratio-distributions.md; f_b table mislabeled "companion frequency" → relabeled multiplicity fraction (matches code). **Batch 3 (profiles) DONE (a26cc83):** King Table II ξ_t = c=4.699/131.4/2272 @ W₀=3/9/15 verified EXACT (re-confirms real log c, not the debunked fake); LIMEPY index g+3/2 correct BUT removed a FALSE "main-text Eqs.8/11 print g+1/2 typo" narrative (held GZ15 p.578 prints g+3/2 directly — prior-session error); CW04 radial Q exact but fractal Q (0.47/0.58/0.70) mislabeled "CW04 Table 1" (real p.590: 0.45/0.61/0.73) → relabeled as the A=πR² estimator output. (First src/ edits — docstring-only, forward values bit-identical, gate unaffected.) **Also (Anna's urgent ask): fixed the GitHub-Actions minute drain (5b32fe8)** — disabled BOTH workflows (`gh workflow disable`) + hardened triggers (tests = PRs-only + cancel-in-progress, dropped the failing nightly `physics-validation` cron). **Batch 4 (Tier 1+2 newly-fetched verify / Bucket C) DONE (11d0fee, ac19066):** D&K91 mass-radius coeffs (1.06 M^0.945 / 1.33 M^0.555) + 1.66 knee verified EXACT vs Table II EMPIRICAL fits — relabeled docstring "ZAMS"→empirical (Anna's call), values bit-identical; all 8 IAU-2009 planet/Sun mass ratios verified vs Luzum 2011 Table 1; planet orbital elements accepted-as-standard-J2000 (Standish/JPL not held, noted); Zahn1977/Lucy2006/King1962/Aarseth+HW1974/Hurley2002 citations all verified APPROPRIATE (rendered the scanned Zahn+Aarseth; fixed Lucy "First"→"systematic study" since the twin hypothesis traces to Lucy&Ricco 1979). **Batch 5 (IMF classic + Sana / Bucket B) DONE (local, awaiting commit):** verified Salpeter α=2.35 (p.165), Kroupa Eq.2 [0.3/1.3/2.3]+breaks [0.08/0.5/1.0] (p.234), Chabrier single-star m_c=0.079/σ=0.69 (Table 1 p.769), Maschberger α=2.3/β=1.4/μ=0.2 (Table 1 p.1727), Sana π=−0.55±0.22/κ=−0.10±0.58/f_bin=0.69±0.09/71% (p.3+Fig.1) — all EXACT vs held PDFs (5-agent inventory fan-out, every value adjudicated by the main loop against the actual PDF). 3 fixes (correct values, wrong/incomplete provenance text): Kroupa ⟨m⟩ range 0.01–1→**0.01–50** M⊙ (p.235); Sana q-slope cite "Eq.3"→"main text & Fig.1" (Science Report has NO numbered eqs); Σ-estimator+k=6 re-credited von Hoerner1963/Casertano&Hut1985 via M&C 2011 (p.544). **KEY (first "value in no held source" of the audit): Sana eccentricity η=−0.4±0.2 is in NO held source** (main paper defers to the paywalled SOM Table S3; absent from all held secondaries — Moe2017/D&K2013/COMPAS×2/Sana-HM2025/Raghavan2010) AND **backs no code** (`MoeEccentricity` computes η from Moe Eqs.17-18, held+Batch-2-verified) → the −0.4±0.2 was docstring/note context only → de-asserted to an honest SOM pointer in all 3 locations (Anna-approved). `make build` 161 pages, **0 warnings**; src/ edits docstring-only → released-core gate unaffected. **Bucket B held-PDF deep-verify COMPLETE.** **Batch 6 (Bucket A phantom-API cleanup) DONE (local):** reframed environment.md's galaxy-wide-IGIMF overclaim → the cluster-scale `BirthEnvironment`/`env_to_imf_params` API (removed phantom `progenax.imf.IGIMF`/`gwimf.sample()`; added a scope admonition: progenax implements ξ_cl, NOT the IGIMF integral); retired the `tests/README` §7 phantom-IGIMF test suite (8 tests that never existed — a guard test `test_documented_api.py` already asserts IGIMF absent) + the `physics-tests` fractal-substructure anchor row; fixed the tidal frontmatter; banner-archived the `phase1-complete`/`PHASE1_COMPLETE`/`2025-12-07-review` snapshots that claimed IGIMF/EnvironmentIMF shipped (root `docs/PHASE1_COMPLETE.md` MOVED to `docs/archive/`). `make build` 161 pages, **0 warnings**; live website/tests phantom-clean. **Bucket A COMPLETE.** **build_plummer_cluster → REAL API (design ratified, `0a7a424`):** brainstormed SoTA design `docs/plans/2026-06-14-cluster-builder-api-design.md` — hybrid generic `build_cluster(profile, ...)` + thin named aliases, new `matched_velocity_df` footgun-killer, anisotropy/tidal/rotation modifier kwargs, dual mass-spec, differentiable θ→ICResult, units-explicit; `builders.py` is over-limit (523 LOC) → new module `builders_cluster.py`; new public entry points must be grad-gate-registered. The `methodology.md`/architecture `build_plummer_cluster` doc snippets are DEFERRED to repoint once it ships. **Batch 7 (LOW spot-sample + consolidation) DONE + AUDIT CLOSE-OUT (local):** LOW-set spot-sample PASS (no systemic rot — Plummer factor is canonical √(2^(2/3)−1), all 116 exports resolve, phantoms absent); consolidation: added 2 grounded per-paper notes (prsa-2016 + kroupa-1995, wired into myst.yml/index nav), harmonized standalone e_jupiter 0.0489→0.04839, removed the confirmed-duplicate Bairagi PDF; clarified false-alarms (szapudi = two DISTINCT papers NOT a dup; bibkeys clean; planet table single-sourced via import; figure-eight already cited Chenciner&Montgomery). NEW ⚠: imf.py:52 cites Kroupa 1995 "MNRAS 277, 1507" but the held + topic-matching paper is MNRAS 277, **1491** (likely typo → recommend 1491; awaiting Anna). `make build` 163 pages (+2 notes), 0 warnings. **AUDIT VERDICT: release-credible — NO systemic rot, ZERO fabricated values in released-core** (every defect was "correct value, wrong/incomplete provenance text" + the de-asserted Sana η + the Bucket-A phantom docs). Full close-out section in `docs/provenance-ledger.md`. Branch `feat/provenance-credibility-audit` ~15 commits, **ALL LOCAL, nothing pushed**; src/ edits docstring/comment/fixture-value only → released-core gate unaffected. **MERGE: recommend merge → local main on Anna's go (no PR, CI minutes out), OR hold until build_cluster lands for a coherent build_plummer_cluster doc repoint — Anna's call.** **`build_cluster` ARC ACTIVE on `feat/cluster-builders` (off main).** Brainstorm round 2 DONE (`ba5ed46`): six OPEN questions ratified one-at-a-time with Anna — (Q1) ship **5 aliases** (added build_michie/limepy_cluster), (Q2) single-population only, (Q3) binaries stay in build_binary_cluster, (Q4) matched_velocity_df first-class public, (Q5/5b) **ADD `ClusterParams` eqx.Module + `build_cluster_from_params`** θ-PyTree wrapper, (Q6) tidal revirialize=False default (+loud S4 docs). 10 new public symbols; verified matched_velocity_df profile→DF mapping + error semantics + 2 caveats (custom-xi_max ODE-domain non-round-trip; LIMEPY is_aniso branch); non-redundant grad-audit scheme (build_cluster=4 modifier cases, 4 alias family cases, build_cluster_from_params=2; build_plummer_cluster+matched_velocity_df EXEMPT_HELPER). **ARC COMPLETE (2026-06-14, 20 commits `ba5ed46..36ba3be`, ALL LOCAL on `feat/cluster-builders`; nothing pushed).** Code-complete 9-batch TDD plan (`e2e2578`) executed subagent-driven with an independent code-review + an Anna HITL checkpoint per batch. Shipped **10 new public symbols** in new module `src/progenax/builders_cluster.py` (243 LOC): `build_cluster` + 5 aliases (`build_{plummer,king,eff,michie,limepy}_cluster`) + `matched_velocity_df` (footgun-killer) + `RotationSpec` + `ClusterParams` + `build_cluster_from_params`. Base case proven **bit-identical** to manual `build_spatial_ic` (pure sugar, exact `==`). Review-driven decisions: tidal guard widened to ALL 4 truncated profiles → `tidal_radius` is **Plummer-only** (I1); `Q` widened to `Optional[float]` (Q=None = faithful unscaled equilibrium); tidal grad is a **straight-through surrogate → teeth-test not an FD-consistent Case** (King grad audits `r_c` not `W0` to dodge the auto-ODE-domain tracer/concrete mismatch). **DoD all green LOCALLY:** unit 43 + integration 7 (Q=0.5000 all 5 profiles) + grad-audit 8 measured cases + tidal teeth-test (registry 77→**85, 0 hazards**, JSON regenerated, ratchet+staleness green) + `scripts/validate_cluster_builders.py` (exit 0: L_z slope rel-err 4.7e-16, β dev 0.019, density recovery PASS) + `scripts/demo_cluster_builders.py` (exit 0, 5 versatility demos incl. θ→ICResult Fisher) + completion doc `.claude-work/TASK_build_cluster_COMPLETE.md`. **FULL released-core gate 1377 passed, 1 skipped, 0 failed (12:30, exit 0).** Validation/demo PNGs untracked per repo `*.png` gitignore (script regenerates deterministically). **FINAL whole-arc adversarial review: READY TO MERGE** (0 critical/important; reviewer independently re-verified pure-sugar bit-identity on 5 untested paths + the 114↔114 SYMBOL_CATEGORY bijection; 2 doc-only minors fixed in `ec83a2e`). **Post-completion pre-release hardening (Anna-directed):** (a) killed 8 leaked `KingProfile r_t`-inconsistency warnings by switching sampling/physics tests to the recommended self-consistent `from_W0_rc` (`747a77f`); (b) consolidated the redundant King unit tests — removed unit `TestSolveKingProfile`+`TestKingPhysics` (test_king.py 19→10; ODE/truncation/concentration/isotropy covered more thoroughly in the validation tier), kept the unit-UNIQUE audit guards (`9bb1f79`); King files verified 45 passed / 0 warnings; suite-wide test cleanup deferred to its OWN arc. **Final FULL released-core gate: 1368 passed, 1 skipped, 0 failed (10:58, exit 0).** **MERGED → local `main` (2026-06-14, fast-forward `f191c4b..af23bbd`, no merge commit; main == the gated commit, so merged-main is gate-confirmed). main is 26 ahead of origin, **NOT pushed** (Anna pushes on her go). **Follow-ups (1)+(2) DONE + MERGED to local main** (`docs/cluster-builder-website` → FF `219d144..303cd2b`): (1) repointed the 3 phantom `build_plummer_cluster` website snippets to the real API (`9faf2b5`); (2) new `50-validation/cluster-builders.md` validation page + 4 curated figures + nav/dashboard wiring (`303cd2b`); `make build` 164 pages, 0 warnings. main now 29 ahead of origin, **NOT pushed**. **Follow-up (3) REFRAMED into a full SoTA pre-release validation-backbone arc (Anna-ratified brainstorm):** one generated+timestamped+gated source of truth for ALL pre-validation checks = 4 registries (differentiability exists; +API-coverage, physics-validation, provenance-of-constants) + a generated dashboard that unions them + a profiling-driven suite refactor (`@slow`-mark the measured sinks — `test_json_fresh` 429s + `test_audit_script` 406s regenerate the same JSON, multimass IFT ~870s; consolidate Plummer/EFF/Michie/LIMEPY cross-tier redundancy like King). Coverage gate = 100% of `__all__` tested + line floor (90%, ratchet-up) + 0 registry holes. **Design `docs/plans/2026-06-14-test-validation-backbone-design.md` (`66143a2`) + 6-phase TDD plan `docs/plans/2026-06-14-test-validation-backbone-plan.md` (`12a732f`) committed.** **REVIEW+VERIFY DONE (2026-06-14, `cbcf0c0` on local main):** adversarial pass (10-agent workflow + first-hand repo verification) → verdict **ready-with-edits**; architecture sound (registry + generated/staleness-gated dashboard + profiling refactor), ALL edits applied to both docs (+134/−57). Fixed: the **coverage.json producer/consumer break** (→ ONE committed full-suite `validation/data/coverage.json` + `coverage_provenance{selector,git_sha}`; floor xfails on a partial selector; staleness checks `git_sha==HEAD`, never re-runs `--cov`); **stale Task-1.1 premises** (pytest-cov already installed 7.1.0 + in `[dev]`; `[tool.coverage]` already exists → EXTEND not add, keep `source=["progenax"]`); the script **sys.path bootstrap**; **LIMEPY not symmetric** (no `test_limepy_physics.py` → separate "inspect, likely no-op" task; verified scope Plummer=partial/EFF=clean/Michie=partial); **API-coverage must map ASSERTING tests not grep-mentions** (theater); `IS_MODEL` defined operationally + cross-checked vs grad-audit `SYMBOL_CATEGORY` (anti-drift); **protect cross-cutting tests** (units/G-threading, protocols, energy) from "kill orphans"; provenance registry = hand-curated `provenance-ledger.md` port, NOT a regex float-scanner (~2,525 noisy matches); re-commit cadence made explicit. **xdist+JAX coverage risk verified BENIGN** (serial vs `-n auto` bit-identical; `branch=true` no false-RED on `lax.cond`). **EXECUTING on `feat/test-backbone` (subagent-driven, TDD, independent code-review + Anna HITL checkpoint per phase; all LOCAL, nothing pushed).** **Phase 0** branch cut. **Phase 1 (keystone) COMPLETE** (`563c2fa`→`2bfb53f` + independent-review fixes `917acce`): generated/timestamped/staleness-gated dashboard JSON (`validation/data/test_dashboard.json`) + MyST page (`50-validation/test-dashboard.md`) + the ratchet (`tests/validation/test_dashboard_fresh.py`); `scripts/build_test_dashboard.py` introspection-only generator; independent review caught 2 gate-integrity bugs (dead provenance teeth, ungated page) — both fixed. **Phase 2 (API-coverage registry) COMPLETE** (`7beee46`→`cea412c`, Anna-checkpointed): `tests/validation/api_coverage/manifest.py` = **112 asserting-test mappings / 2 EXEMPT / 0 untested = 114** (NOT grep-theater); full-suite `coverage.json` **94.77% ≥ 90% floor** (hard, provenance-stamped); **src-based coverage freshness** ratified by Anna (replaced unworkable git_sha==HEAD: stale iff `src/progenax/` changed since measurement); 6 analytical-IC holes filled with real-physics tests (ẍ=−ω²x ODE-satisfaction, vis-viva, Kepler-III, COM, figure-eight literature constant). **2 of 4 registries built+full** (differentiability + api-coverage). **Phase 3 (suite refactor) DONE** (`026aae2` @slow-marked the 2 grad-audit JSON regenerators via a shared session-scoped regen, ~400s off the FULL gate; `37b8f48` consolidated **11 cross-tier-redundant unit tests** — Plummer 4 / EFF 5 / Michie 2, each Anna-approved per-item, physics subsumed at ≥ rigor in validation tier). **Phase 4 (physics-validation registry) DONE** (`6f922ab`→`7841546`): `MODEL_INVARIANTS` manifest (21 models→invariants) + **operational `IS_MODEL` ratchet** (protocol-conformer or `build_*_cluster` → a new model can't escape) + 3 reference-parity carve-outs (LIMEPY×2, UniformSphere). **Phase 5 (provenance registry) DONE** (`1db86d4`→`bcad593`): ledger-ported constant→citation manifest (29 constants, 8 allowlisted modules) + allowlist-scoped ratchet, **0 unprovenanced** → **`registries_full=True` (all 4 built+full)**. **Phase 6 (docs+close-out) DONE** (`ea32a0f`→`030e98a`): new `50-validation/testing-architecture.md` (3-tier + tests-vs-registries + 4 registries + 4-part release gate; warning-clean after `4c79a32` dropped the unsupported `:widths:`); `c41ecf5` retired the hand-maintained index.md "Status dashboard" drift table → pointer to the generated dashboard (Anna chose TARGETED retirement, kept curated prose) + referenced registries in `tests/README` + dropped the dangling `test_gamma_affects_concentration` ref; `030e98a` **deferred coverage refresh** — authoritative final-tree full `--cov`: **94.77%→95.74%** (confirms the 6 hole-fills added real coverage + the 11 removals lost NONE), re-stamped coverage.json + re-emitted dashboard, staleness gate green. **ARC COMPLETE — release gate all-green LOCALLY at HEAD `030e98a`: registries_full=True · line_cov 95.74≥90 · dashboard fresh (staleness 3 passed) · FULL suite 1408 passed/1 skipped/0 failed (12:13).** 21 commits `563c2fa..030e98a`, 37 files (+46,868/−304), ALL LOCAL on `feat/test-backbone`, nothing pushed. Completion doc `.claude-work/TASK_test_backbone_COMPLETE.md`. **CHECKPOINT 6 → MERGE `feat/test-backbone` → local `main` on Anna's go (fast-forward, NO push — CI minutes out).** Deferred future arc: the suite-wide test-cleanup (high-value-only, @slow over delete). See [[test-backbone-arc]]. HITL; verified LOCALLY; NO push/merge without Anna's go.
blocker: none (active; Anna adjudicates per-batch flags/commits).
due: Anna's per-batch approvals.

PRIOR next: **Gradient-gate LOCK-IN + COMPLETION arc — Phases A+B DONE, Phase C (close-out) in progress (2026-06-13, branch `feat/gradient-gate-lockin` off main).** Turned the differentiability gradient-gate from a one-time audit into a **permanent, self-policing, complete** invariant (design `docs/plans/2026-06-14-gradient-gate-lockin-{charter,design,plan}.md`; six design Qs Anna-ratified). **Phase A (machinery):** new `tests/validation/grad_audit/manifest.py` — three frozen literals: `MUST_AUDIT` (the (id,param) coverage ratchet target), a complete `SYMBOL_CATEGORY` map over all 104 `progenax.__all__` symbols (AUDITED | EXEMPT_*), and `PARAM_ALLOWLIST` (carry-forward known-limitations). THREE enforcement layers, all pytest tests in a dedicated fast `gradient-gate` CI job (wired into the `tests` aggregator): **(1)** the run-gate (`test_grad_audit.py` + `scripts/audit_gradients.py` exit-0), **(2)** a Layer-2 staleness comparator (`test_json_fresh.py` — regenerates the JSON and semantic-diffs vs the committed artifact: exact on discrete/structural fields, rtol on floats for cross-arch; hardened with a non-vacuous floor + isfinite branch + tol-in-discrete), **(3)** a Layer-3 coverage ratchet + `__all__` cross-check (`test_manifest_coverage.py` — a deleted case OR a new/ungated public entry point reds CI). Each layer's teeth demonstrated RED→GREEN. CHECKPOINT A1 (Anna): promoted `LogisticThermalEccentricity` + root-exported the rotation overlays into `__all__`. **Phase B (D4 completion, per-target RED→GREEN, measured ≥3 seeds, never-weaken-tol):** closed ALL deferred Fisher-coverage holes — Engine-A `w_j` (from_components) + `r_a` (from_mass_segregation), `build_binary_cluster` end-to-end (machine-exact, r_h-linear), the 3 period dists (Sana/LogNormal→1/LogUniform→½), the 4 ecc dists (Thermal→⅔/Uniform→½/Moe/LogisticThermal), and `IndependentCompanions.e_max` (through the full assembly). **11 new cases, registry 66→77 rows (73 clean, 4 pinned known-limitations, 0 hazards)**; every case code-reviewed (teeth verified, tols measured-honest). **CI minutes exhausted → verified LOCALLY: full released-core fast gate 1267 passed (399s, exit 0).** **Phase C in progress:** regenerate JSON+plots, website Tier-5 + manifest-coverage section, STATUS/brain, DoD; **NO PR (CI minutes out) — close out by local merge on Anna's go.** See [[gradient-gate-lockin-arc]].

PRIOR next: **Differentiability gradient-audit arc COMPLETE (Tiers 0–4, 2026-06-13, branch `feat/differentiability-audit`).** The grad-audit registry is now the **release gradient-gate**: `tests/validation/grad_audit/registry.py` → **66 Cases** (62 clean, 4 pinned known-limitations, **0 unresolved hazards**), asserted by `tests/validation/test_grad_audit.py` (**67 passed** = 66 + a stop_gradient mutation/teeth test) and regenerated into the single-source `validation/data/grad_audit_results.json` by `scripts/audit_gradients.py` (exit 0). Premise: gradient integrity = Fisher integrity — a silently zeroed/NaN gradient corrupts a Fisher matrix with no error raised. **Two real silent hazards found + fixed** (both gradient-only, forward bit-identical, Anna-ratified): King tidal radius `r_t` silent-zero across King/Michie/LIMEPY/multicomponent via unclamped-ψ (`9d3365a`); segregation surrogate `stop_gradient` ~27% bias at β=0.1 (`6fa665c`). **Tier 4 (Task 4.1–4.3)** turned the registry into the release gate: inventoried every released-core gradient assertion, **migrated ~24 duplicate AD-vs-FD checks** in (scattered tests now carry registry-pointer comments), **deleted ~15 finite-only `isfinite(grad)` smoke tests** (audit finding T6 — a silent-zero passes them), **added 9 new Cases** (King/EFF DF scale params, `PowerLawIMF.ppf(m_min)`, the 3 Kepler `to_state`/`from_state` Jacobian columns, the Michie density observable; 57→66) and **fixed the H6 boundary probe** (folded edge → real u=1e-12 standalone Case). The interlock surfaced 2 real gaps (Michie density-W0, EFF positions-a) → added as Cases. **Task 4.3 final artifacts (this session):** `--plots` flag on `scripts/audit_gradients.py` renders `validation/plots/grad_audit_{ratio,summary}.png` (+ committed website copies under `docs/website/50-validation/figures/`) straight from the JSON; website doc `50-validation/differentiability-audit.md` reconciled to 66 cases with a new Tier-4 section + both embedded figures + changelog (**myst build 161 pages, 0 warnings**); completion doc `.claude-work/TASK_differentiability-audit_COMPLETE.md`; inventory `.claude-work/grad-test-inventory.md` (CHECKPOINT 4a ratified). `enclosed_mass_fraction` (Tier-3 public CDF, model-side N(r) Fisher channel) has 12 unit tests. **MERGED into `main` via PR #8 (merge `2164a7f`, 2026-06-14) — all 9 CI checks green, `feat/differentiability-audit` deleted (local + remote), worktree clean on main. CHECKPOINT 4b closed; the arc is DONE.**

PRIOR next: **Batch C — Science Demos II COMPLETE (2026-06-12, branch `feat/batch-c-demos` off merged main `7d1f402`, 14 commits `8e52936..050dd5e`; plan `~/.claude/plans/twinkling-knitting-quilt.md`, completion `.claude-work/BATCH_C_SCIENCE_DEMOS_COMPLETE.md`).** Six gated CLI demos (all ALL PASS, run-records captured) across the four undemoed threads, paper-seed + methods scope: **B9** binary energy budget (Q_com=0.5000 clean; |E_internal| dwarfs |W_com| 12–1900×; Q_resolved phase-contaminated not "deflated"; EFF-vs-King environment fig, same Moe pop same key → identical E_internal, reservoir 440 vs 298); **B11** King (W₀,r_c) from binned STAR COUNTS alone (6.0→6.028±0.024 / 1.0→0.995±0.010; c(W₀) 1.263 vs Tab.II 1.255; honest ρ(W₀,r_c)=−0.91 count-channel degeneracy) — validates the NEW Poisson number-density harness channel; **B10** differentiable diagnostics (q_approx vs exact Q <0.06 in substructure regime; Λ_approx rank-faithful Spearman 1.00; q_approx AD-vs-FD 4%; Plummer Q>1 excluded honestly); **B5** birth-environment archaeology (α₃ measurable ~40σ at N=1e4, CRLB empirically validated; environment RANK-1 unrecoverable, Fisher eig [−1.8e-12,1.8e-12,1.4e4]); **B4** unresolved-binary mass function — the headline (recover f_b from blend distortion via Tout+1996 ZAMS from fluxax; ignoring Moe P–q coupling biases f_b −3.6σ ~5% low at survey scale; blended median q 0.540 Moe vs 0.479 shuffled); **B6** anisotropy (OM r_a 1.5→1.55±0.035 recovered; OM-fit to a Michie cluster → reduced χ² 5.58 = 12.9× inflation + r_a mis-est 8.9 vs 6.0 — formalism matters). **STRETCH (also done): B7** tidal radius from the count-limited outskirts (r_t 12.0→12.01±0.04, 93% Fisher info from the outer bins, Jacobi → R_gal 2.79±0.01 kpc; r_t made differentiable via cumulative-clip since a hard `where` truncation has ZERO gradient through its boundary); **B8** rotation+projection — ⟨v_los⟩ measures the product ω·sin(i) (1.69±0.03) not ω, the (ω,i) Fisher is rank-1 (cond 2.3e16), introduces a line-of-sight projection helper (realism-axis bridge). All 8 demos' figures VISUALLY INSPECTED (publication-style, physically correct; B5/B6/B7/B8 polish fixes applied). Harness: NEW Poisson number-density trio in `_demo_inference` (binned_number_density / poisson_loglike / poisson_fisher_information, 11 tests, 28/28). Infra: **fluxax wired CI-safely** (private sibling, NOT locked — documented [experimental] note + guarded import + local `uv pip install -e ../fluxax --no-deps`; uv.lock unchanged, `uv lock --check` green); `docs/website/Makefile` (--no-deprecation); latent Batch-B figure-tracking gitignore gap fixed (B1–B3 figures were never committed); peuten YAML + Tout1996 bib. myst **157 pages, 0 warnings**. Released-core gate unaffected (Batch C touched only scripts/+docs/+a pyproject comment; src/ untouched) — FULL gate re-run at close-out. **blocker: AWAITS ANNA — ONE final PR for `feat/batch-c-demos` on your go (per the project workflow). All 8 demos done (6 core + 2 stretch); future = the observational-realism arc (measurement errors/selection/PSF across all demos, B8 introduced the projection bridge). due: Anna's go.**

PRIOR next (audit-fix arc COMPLETE + MERGED to main via PR #6, 2026-06-12): **Pre-release audit fixes COMPLETE (2026-06-12, branch `fix/prerelease-audit`, 27 commits `7fe4cec..86339d1`; plan `docs/plans/2026-06-12-prerelease-audit-fixes.md`, completion `.claude-work/PRERELEASE_AUDIT_FIXES_COMPLETE.md`).** All R1–R10 + S/J/A/T/D blockers fixed via strict RED→GREEN TDD. CI un-broken (lock decoupled from the private jaxstroviz sibling) + un-OOM'd (unit-tier sharding + CI-gated jax.clear_caches) + new wheel-smoke/nightly-version-matrix/strict-refs lanes; Moe F_twin q>0.3 (0.367→0.300), King/Michie/EFF core grid (+270%→<0.1%), sample_fixed_n guard, D&K91 radii, α=1 grad, J4/J5/J6/S1/S2/A1, honest equilibrium caveats; R7 README rewrite + executable-examples smoke test, phantom IGIMF/EnvironmentIMF + progenax-legacy claims killed, counts reconciled; Apache-2.0 LICENSE + wheel metadata, [diagnostics] extra + lazy scipy, CHANGELOG. FAST gate 1191 passed (exit 0); myst 151 pages 0 warnings; wheel metadata verified. **blocker: AWAITS ANNA — (1) R2 pip-installability strategy (publish jaxstro to PyPI / vendor units+jaxconfig / git-URL dep); (2) merge to local main + ONE final PR when CI is green. due: Anna's go.**
PRIOR next (audit-fix arc now complete): **Triage the 2026-06-11 pre-release adversarial audit** (`docs/reviews/2026-06-11-prerelease-adversarial-audit.md`, 6 parallel specialist agents): verdict **Beta, ~2 weeks from RC**. Top blockers: CI RED on main (missing jaxstroviz sibling checkout — zero tests enforced for ≥5 pushes); slow-marked headline validations (LIMEPY parity, A-vs-B) never run in CI; Moe F_twin normalized against q>0.1 instead of the paper's q>0.3 (+22% twins, measured); King/Michie CDF grid under-resolves the core at W0≳9 (+18% enclosed-mass error at 0.3 r_c, measured); `sample_fixed_n` silent 30% mass shortfall (zero tests); `compute_stellar_radii` exponents inverted vs MS homology; README teaches deleted `TwoComponentConfig` + phantom IGIMF/EnvironmentIMF; wheel uninstallable (unpublished jaxstro dep, no LICENSE). Then: **Merge/push decision — AWAITS ANNA'S GO** for `feat/batch-b-demos` (and the earlier `feat/multimass-limepy-validation`); then the gravax forward chain (render recovered ICs) OR paper assembly — Anna's call. **DONE (2026-06-11, branch `feat/batch-b-demos`, commits `760e086..15e112e` + close-out): Batch B science demos Tasks 6–9** (plan `docs/plans/2026-06-10-batch-b-science-demos.md`; completion doc `.claude-work/BATCH_B_SCIENCE_DEMOS_COMPLETE.md`). **B2d** wrong-IMF bias (dδ̂/dα = −0.0047 ± 0.0195, δ̂ PEAKS at the true α; REPORTED not gated — adjudicated the α=2.1 scatter as real realization noise, not optimizer artifact) + robustness grid (α_true 1.9/2.3/2.7 all within 3σ). **B3** halo+core Engine-B (t, r_a, r_h) recovery (0.602±0.008 / 3.051±0.053 / 2.015±0.029, all <1σ; 3 inits→identical loss 19.1479; realizability f_min 1.16e-4; binned-expectation σ + closed-form OM β). **Website** `60-science-demos/` (4 pedagogical pages + 6 figures wired after Validation; `myst build` exit 0, 0 warnings, **151 pages** — the plan's 148 assumed a stale 144 baseline). **Close-out**: FULL released-core gate **1192 passed (9:07), exit 0**; all four demos (B1/B2/B3/B2d) re-run exit 0 ALL PASS, fresh-captured. Integrity catches: B3 docstring had a FABRICATED "measured" runtime (the subagent never completed a run → corrected to measured 3.7 min, MLE-scan-compile dominated); B1 lowered-DF eq index is g (NOT g+½ — verified vs GZ15 eq 2 / limepy.py, King limit E₁=eˣ−1); independent B3 review → occupancy-STOP now exits before the figure. **DONE (2026-06-11, branch `feat/multimass-limepy-validation`, commits 0bb5cc0..f0f1722): Multimass LIMEPY validation arc** (plan `docs/plans/2026-06-11-multimass-limepy-hardening-plan.md`; completion doc `.claude-work/MULTIMASS_LIMEPY_VALIDATION_COMPLETE.md`). Phase-1 PDF verification found+fixed TWO doc equation errors (Peuten eqs 8–9 ratio was inverted — paper reads (m̄*/m̄)^{2δ}; Bianchini A1 prefactor is μ^{−δ} and the paper itself omits the ^{1/2} on the E_γ ratio, confirmed via its own A2). **Task 1 parity**: Engine A ≡ canonical reference LIMEPY (SHA ef2a479, `meanmassdef='central'` ⇒ like-for-like, no eq-8/9 translation) to ≤1.5e-4 across 6 configs (single-mass g=1/1.5, 2-comp, B2 4-comp Maschberger, aniso η=0/0.5); gates measured-then-frozen ≤5e-4 (20× under the plan's 1% target); the reference runs ONLY in a pinned-env subprocess (py3.11/numpy1.26.4/scipy1.11.4 — project scipy 1.17 breaks it) with a committed npz cache + skip-if-absent slow test (3 tests). **Task 2 (Anna-reviewed → FIT-FREE)**: equipartition saturation validated by exact statements only — solver≡closed-form A1 to 1.6e-7; HEADLINE μ→0 identity m/(2η)≡derived m_eq=m̄(g+5/2)(g+7/2)/φ̂₀ to 0.11–0.19% (W0=5/7/9); η(m) shape via autodiff (flat 0.026–0.039 at m_min → ½ exactly deep-well). KEY SCIENCE: the DF saturates SHARPER than exponentially (Bianchini A2's quadratic coefficient is NEGATIVE, opposite eq-3's) ⇒ finite-window eq-3 fits recover ~0.5× the derived m_eq (0.50–0.53 over μ≈[0.14,1.7]; →1.0 as the window shrinks) — documented as a literature caution (window-match before comparing; future-work idea: fit the exact differentiable σ(m;g,W₀,m̄) to kinematic data). **Task 3 docs**: NEW theory page `10-theory/spatial-profiles/multimass-equipartition.md` + corrected per-paper notes wired into nav (myst 147 pages, 0 warnings); docstring-only src edit (GZ15 §4 √-update named). **Close-out**: FULL gate **1192 passed** (= main baseline 1189 + 3 parity tests; CLAUDE.md's "1163" is a stale count). meq/zeta stay DEFERRED. **DONE (2026-06-11, merged+pushed): Multicomponent Engine A IFT hardening (H1–H5)** — `find_alpha_for_masses` → hand-rolled `custom_vjp` (adaptive while_loop fwd + reverse-mode implicit-VJP backward), ~9× faster B2 demo, exact grads (AD-vs-FD iso 2e-7), jit(grad) pinned, FD-gate tightened 1e-6; review caught+fixed a Critical jit(grad) regression. **+ Batch B Tasks 1–5 DONE:** B1 cross-engine figure; B2 joint (α,δ,W₀) MLE recovering reference θ̂ bit-for-bit; Task 5 Fisher information panel (kin-only ρ(α,δ)=−0.26 mild, mass channel PINS α 4.7×) + NUTS corner (0 divergences, posterior-mean within 1σ of MLE, flat-in-θ target). **+ equation-verified per-paper notes** (Bianchini2016, Peuten2017 new; GZ15 updated) documenting the multimass methods/theory. Replaced the unrolled 30-iter eigenvalue scan in `find_alpha_for_masses` with a hand-rolled `jax.custom_vjp` (adaptive `while_loop` forward to tol=1e-6 + reverse-mode implicit-VJP backward of the √-map residual R; TWO solvers dispatched on `ra_hat is None`). ~3× faster per `value_and_grad`, flat-in-n_iter, gradients exact (AD-vs-FD iso 2e-7, aniso quadrature-oracle 5e-5; jit(grad)/jit(value_and_grad) pinned). The B2 demo (`demo_delta_recovery.py`) right-sized 4×600→3×300 → **5:42 vs ~50 min (~9×)**, recovers the gold reference θ̂=(α 2.2931±0.0041, δ 0.3972±0.0344, W₀ 4.9900±0.0209) **bit-for-bit**, OVERALL ALL PASS. Review cadence caught + fixed a Critical `jit(grad)` regression (`jnp.asarray(eta)` in a nondiff_argnums slot) at H2, two tasks before the demo that would have hit it. optimistix REJECTED (its JVP adjoint forces diffrax DirectAdjoint → slower). Fresh gates: **full released-core 1188 passed (7:17)**, memory 7/7 PASS, box-corner harness 3/3 (residual 5–8e-7), demo exit 0. Completion doc `.claude-work/MULTICOMPONENT_IFT_HARDENING_COMPLETE.md`. Future IFT candidates noted (Engine B trunc radius, King tidal radius). Commits `dd14713..ccf6ed1` (6) + harness/tests. **DONE (2026-06-10, Anna-approved): DF sampler-fusion micro-batch** (profile-driven: staged RSS decomposition showed construction ~1.7 GB dominates, eager draw +0.63 GB): speed-CDF tables cached at construction + jitted sampling cores (eqx.filter_jit) for LIMEPY/King/Michie standalone DFs — warm aniso draws **0.35 s → 15 ms** at N=2e4 (3–16 ms across N; 55× vs pre-batch quadrature at N=1e4), draw-chain peak +0.63→+0.20 GB, stage 2.37→2.02 GB, `limepy_df_aniso` gate tightened 3.0→2.5 GB, small-N build penalty eliminated; lax.map chunking of the table draw evaluated and rejected on measurement. Fidelity re-verified on the jitted path (identical KS/moment/β, ALL PASS). Gate: **1163** released-core (unit 895; fast 1121). Remaining DF-stage floor is model CONSTRUCTION (~1.8 GB, paid once per model) — future target if ever needed. **DONE (2026-06-10, Anna-approved): Batch A merged to local main** (fast-forward to `2263948`; FAST gate re-verified on merged main 1108; main NOT pushed — 22+ ahead of origin, awaits Anna's go) **+ the performance-evidence addendum** (Anna-requested, plan-approved): `scripts/benchmark_batch_a.py` measures BOTH code versions live (pre-batch worktree at `0dd1cd9` vs HEAD) — wall-clock: PE 2.4× faster at N=8k (0.169→0.070 s), N=2e4 feasible at 0.216 s (dense: 20.9 GB, infeasible), honest small-N regressions measured (PE 3.5× slower at N=2k; DF table-build dominates below ~5k); memory: 16× at N=8k (gated ≥5×); fidelity figures table-vs-quadrature ALL PASS (KS ≤ 6.7e-3); NEW `50-validation/performance-memory.md` page (myst 144 pages, 0 warnings) + King/Michie fidelity embeds. The multi-component cluster arc is DONE end-to-end: Engines A (LIMEPY DF tables) + B (density-defined Eddington) merged to main (`4f4f259`), the post-merge code-review fix batch landed (`0dd1cd9`), and **Batch A (memory + consolidation + docs)** is complete — blocked+remat'd virial kernels (forward AND grad O(block·N)), table-routed standalone LIMEPY/King/Michie DFs (quadrature kept as the `speed_method="quadrature"` oracle), `progenax.numerics` consolidation (cumtrap ×5 + inverse-CDF draws), `_EngineAState` grouping (A-only access on B raises informative AttributeError), shared angular conditional in `kinematics/_speed_kernels.py`, `lax.map(batch_size=2048)` bounded oracles, and the docs metrics refresh. **Memory headline (measured, `scripts/profile_cluster_memory.py`, all PASS): virial kernel at N=2e4 32.8 GB → 0.41 GB; standalone anisotropic DF sampling 10.9 GB → 2.3 GB; Engine A/B builds at N=1e5 ≤ 2.5 GB.** Gate: released-core **1150 passed** (unit 882 / integration 34 / validation 234; fast gate 1108). **Merge/push of `fix/memory-and-consolidation` awaits Anna's go.**

PRIOR (collapsed; superseded-arc detail in `docs/plans/` + `.claude-work/`): **UNIFIED MULTI-COMPONENT CLUSTER REDESIGN on branch `feat/multimass-limepy-equilibrium`.** An 8-dimension adversarial review of the two-component/segregation code found ONE real physics bug (`generate_two_component_cluster` feeds the FULL mass to each sub-population's velocity DF + no COM/virial rescale → non-self-consistent for unequal-mass IMFs; silent because tests use `jnp.ones(N)`) + an ad-hoc API; the `lambda_seg` blend + `energy_sorted_segregation` are sound. **Decision (Anna, pre-launch, no backwards-compat):** build ONE unified differentiable `MultiComponentCluster` (eqx.Module → ICResult) spanning mass-seg · GC 1G/2G · binaries-vs-singles · halo+core, via TWO engines — **Engine A** (DF-defined lowered-isothermal/LIMEPY, mostly built) + **Engine B** (density-defined Eddington/OM for Plummer/EFF/King). Keep `energy_sorted_segregation` as a labeled PRIMORDIAL generator; **retire the `lambda_seg` blend**. Subsume+retire `MultiMassLIMEPY` (mass-seg → a `.from_mass_segregation()` constructor). Plan: `~/.claude/plans/we-are-continuing-progenax-s-crispy-rain.md`. KEY INSIGHT: a component's single free Engine-A scale is the velocity-scale ratio `w_j=s_j/s` with `rescale_j=w_j^-2`; mass-seg is `w_j=μ_j^-δ`. **DONE: Phase 1a** — extracted `solve_multicomponent_limepy(alpha_j, rescale_j, W0, g, ra_hat_j=None)` core (direct per-component scales); `solve_multimass_limepy` is now a thin mass-wrapper (bit-identical, verified); +5 TDD unit tests; committed `47dc24a`. **DONE: Phase 1b (commit `13de746`, NOT pushed)** — `progenax.cluster.multicomponent.MultiComponentCluster` (eqx.Module): physics flows from `(w_j, rescale_j=w_j^-2, ra_hat_j)`, `m_j` decoupled (labels only); `.from_components` (general: GC 1G/2G, halo+core) / `.from_mass_segregation` (w_j=μ_j^-δ) / `.from_imf`; `sample_cluster → ICResult` with NEW optional `component_id` field; the NEW unequal-mass two-population regression passes (theory Q_j=0.5±2e-3, sampled global ±0.04, per-component ±0.07); `MultiMassLIMEPY` RETIRED (tests ported, validation file + both `validate_multimass_*` scripts rewired and re-run PASS, exports updated). Released-core **1065 passed, exit 0** (1056 at 1a − 14 ported-out + 23 new). **DONE: speed fixes** (commit `3f15479`): pytest-xdist + XLA thread caps + `slow` markers (FAST/FULL gates in CLAUDE.md); solver `rho_j_grid` reuse; jitted sampler core. **DONE: Phase 1.5 — LIMEPY DF tables** (Anna-approved plan `docs/plans/2026-06-09-limepy-df-tables-phase15.md`, subagent-driven with per-task independent review; commits `dda2887..f69f36d` + close-out, NOT pushed): three differentiable table primitives in `profiles/limepy_tables.py` (`AnisoDensityTable` (√W, asinh p) cubic-Lagrange; `SpeedCDFTable` 256×256; `AnisoSpeedCDFTable` 192×48×192 speed marginal with EXACT angular conditional) replace pointwise quadrature in the coupled-Poisson RHS + both sampler branches. **Measured: aniso construction 957→170 ms (5.6×); sampling at N=1e5: iso 67×, aniso 21.7× (0.48/2.9 µs/star); aniso small-N break-even ~3k stars (documented).** Every approximation budget-asserted with the exact quadrature retained as a selectable oracle (`aniso_method="quadrature"`) — density 6.05e-6 (1e-5 budget), solve |Δψ|≤1.93e-4 over 3 configs, CDF 4.5e-5, speed moments ≤0.28%/1.5% vs DF quadrature, β(r)≡DF <0.06 unchanged, AD-vs-FD 2.15e-4 + table-AD≡quadrature-AD 2.1e-4; `component_virial_ratios` deliberately quadrature-ONLY (oracle independence: table-built model proves Q_j=0.5001±1.5e-4). Reviews caught+fixed 3 real bugs pre-merge (W≤0 NaN gradient at the truncation boundary; high-g (≥2.5) CDF-row underflow corrupting outer-shell speeds, now gated g∈{0,2.5,3.5}; the plan's own √(2π) test bug). `scripts/validate_df_tables.py` ALL PASS; completion doc `.claude-work/TASK_1.5_DF_TABLES_COMPLETE.md`. Released-core at 1.5 close-out: 1098 (gate evidence in `.claude-work/TASK_1.5_DF_TABLES_COMPLETE.md`, commit `a85bada`). **DONE: Phase 1c (commits `b1caabb`, `8cda23d`, + 1c-3; NOT pushed)** — the Anna-approved OUTRIGHT DELETION (−3.4k LOC): `populations.py` (the full-mass-to-each-DF physics-bug module) + `cluster/core.py` (string-dispatch `generate_cluster_ic`/`ClusterState`/`SpatialStructureParams`/`MassSegregationLayer`) + `cluster/validation.py` + all `lambda_seg` machinery/scripts RETIRED; `test_segregation_equilibrium_physics.py` PORTED onto the primordial generator via the protocol API (λ=1 endpoint Q_j drift<0.08, orbit-reuse regression, Λ_MSR rises); `validate_cluster_ic.py` §2 rewired to `MultiComponentCluster` (cold w=0.7 median r 1.50 pc < hot 8.36 pc, oracle Q_j=[0.5,0.5], ALL PASS); `mass_segregation.py` relabeled PRIMORDIAL (documented departure from Baumgardt+2008 random per-bin draw; wrong MNRAS 384,1231 cite died with populations.py; dead `_mcluster_partial_shuffle` removed); **eqx migration complete — src/ is dataclass-free** (`ICResult`/`Systems`/`Stars`/`TotalMass`/`AnalyticalIC`/`RotationParams`/`VelocityModel` now eqx.Modules; physical scalars stay differentiable leaves, control-flow config static; jit-over-ICResult verified); `compute_profile_potential` docstring fixed (SPECIFIC potential, [length]²/[time]²). Full gate **1064 passed in 438s** (unit 802/integration 34/validation 228; fast gate 1029; exact count reconciliation 1098−28−7−4+4+1). Phase-3 note: 16 docs/website source pages still reference the deleted API (inventoried; the docs sweep is Phase 3 scope). **DONE: Phase 2 — Engine B (density-defined Eddington), commits `292b6b0..08e328b` + close-out, NOT pushed:** `MultiComponentCluster.from_density_profiles(profiles, mass_fractions, m_j, r_a_j=None, r_t=None)` — Plummer/EFF/King DENSITY components in ONE shared self-consistent potential (no ODE: one cumtrap Poisson pass, new `profiles/density_poisson.py` with derived-domain provenance + King-override conflict rule), per-component DFs via the generic `eddington_invert` (extracted from the EFF table BIT-IDENTICALLY, pinned; + Plummer E^{7/2} (untruncated zero point) and EXACT truncated closed-form oracles, 1.06e-4/8.86e-6), optional per-component OM `r_a_j`, f_j≥0 realizability gate (concrete builds refuse naming the component + f_min + remedy; traced builds store the `f_min_j` diagnostic). Anchors ALL PASS (`scripts/validate_multicomponent_eddington.py`, 11/11): King A-vs-B (TWO independent engines: σ_1d(r) dev 3e-4, radial KS 2e-4, both gates 0.02), EFF(γ=5)≡Plummer twins, halo+core headline (theory Q_j=0.5±4e-4; sampled global Q=0.4976 UNSCALED at N=30k; sampled per-component Q_j ≡ the hybrid predict-the-offset quadrature expectation — the hard-truncated halo's plateau 0.4953 predicted / 0.4947±0.0014 sampled (18 seeds) is VERIFIED truncation-edge physics, gated on the prediction, Anna-approved), OM β_halo(r)≡r²/(r²+r_a²) (max dev 0.028 < 0.05), DF-density fidelity 1.06e-3 (OM) / 2.4e-4 (iso) vs 5e-3 gate, AD≡FD 5.6e-9/7.8e-7/2.0e-8 (r_h, mass-fraction t, r_a). THREE physics findings: (1) the E^{7/2} oracle needs the UNTRUNCATED zero point (truncation shifts E by c=M/√(r_t²+a²), ~30% at 0.1Ψ₀); (2) the plan's default a_EFF=0.4 core is GENUINELY unrealizable (f_min −0.20; gate flip measured a∈(0.65,0.68); headline uses 0.8); (3) hybrid-sampling Q_j plateau predicted+verified. Plus the King dW/dr staircase bug (jnp.gradient of interpolated ψ → Eddington ringing, f_min −0.679) fixed via King's own Poisson identity → +5.1e-7. Full gate **1101 passed in 511s, exit 0** (unit 833, integration 34, validation 234; fast gate 1059); close-out `.claude-work/PHASE_2_ENGINE_B_COMPLETE.md`. **DONE: merge + push (2026-06-10, Anna-approved):** `feat/multimass-limepy-equilibrium` fast-forwarded into `main` (`18f116b..4f4f259`), merged-main gate re-verified **1101 passed**, pushed; local branch deleted (stale remote branch copy left — remote deletion not authorized). **DONE: Phase 3 docs sweep (2026-06-10):** comprehensive website update — `lowered-model-family.md` flipped roadmap→implemented Engine A methods page; populations section rewritten around the two-engine `MultiComponentCluster` (+ NEW `10-theory/populations/eddington-engine.md`); API reference regenerated (PUBLIC_MODULES −populations +cluster/diagnostics/dynamics; 11 module pages, 175 symbols; `30-api/populations.md` deleted); NEW `50-validation/{multimass-equilibrium,engine-b-eddington}.md` with Measured tables + figures (stale mass-seg numbers corrected by re-running validate_cluster_ic.py: Λ_MSR 4.74, ρ(m,E)=−0.92); all 16 inventoried stale-API pages fixed (dev-log: dated retirement admonitions, history preserved); myst build clean (142 pages, 0 warnings). **DONE: /code-review of the merged arc (7 finder angles + verification):** 3 CONFIRMED post-merge correctness bugs — (1) NaN-blind realizability gate (`nan < -1e-3` False; r_a_j=0 → silent all-NaN model), (2) no mass_fractions/m_j/r_a_j length validation vs profiles (phantom components via clamped gathers; potential built from wrong total mass), (3) absolute Ψ thresholds in `sample_speed_from_f_table` are scale-dependent (r_h≳1e4 pc → exact-zero speeds; 80% of stars at r_h=1e6) — plus `total_density()`/`rescale_j` silent-NaN on Engine B, `is_aniso=False` lie for OM-B models, `r_t_provenance` static field embedding float values (per-r_t recompile + vmap break), and a consolidation list (DF speed-moment kernel ×5 copies w/ inconsistent n_w, cumtrap Poisson pass ×3, King density/derivative split, union-by-NaN → _EngineAState grouping). All documented honestly in the new docs limitations admonitions. **RESUME AT: the code-review fix batch (findings 1–3 first; then 4–6; consolidations after), then cross-engine validation figures + science demos.** HITL: push only when Anna says.

PRIOR next (superseded by the redesign above): **LIMEPY multi-mass equilibrium arc — Phases 0–2b + anisotropic IC sampler DONE. Released-core 861→1051 (verified `1051 passed`, exit 0). Pushed.** **Phase 2b (per-component anisotropy η)**: `solve_multimass_limepy`/`MultiMassLIMEPY` take `ra_hat`,`eta` → per-component r_{a,j}=r_a·μ_j^η; aniso density via the stable T(β) quadrature; theoretical Q_j=0.5 holds with the anisotropic v² moment; differentiable in (r_a,η,δ). **Anisotropic IC sampler**: `sample_cluster` aniso branch reuses the validated `_sample_speed_angle` Michie/OM kernel (per-star W_i=rescale_i·ψ, p_i=(r/r_c)/(r̂_a·μ_j^η), v=s_i·(u_r r̂+u_t t̂)). Validated: global Q=0.5 across δ (scalar virial is anisotropy-blind); sampled β_j(r) ≡ DF's own (u,c)-quadrature β across resolved bins (rise to ~0.22 peak near 0.5r_t + truncation TURNOVER — the LIMEPY signature); edge β is finite-N ratio-noise (⟨v_r²⟩→0), shown converging to DF under seed-averaging (16-seed: 0.65–0.75r_t→0.191±0.012 vs DF 0.193). +5 unit + 1 validation test; scripts/validate_multimass_anisotropy.py (seed-averaged β±sem) PASS. Partial-equilibrium hardening of mass segregation: a first-principles, differentiable, multi-mass lowered-isothermal family (Gieles & Zocchi 2015) that COMPLEMENTS the `lambda_seg` blend. **Phase 0**: per-mass-group virial diagnostic `per_group_virial_ratio` (dynamics/virial.py) + FIXED a released bug (`energy_sorted_segregation` orbit-reuse → coincident stars/V=−∞ for steep IMFs at λ=1; deterministic monotonic assignment) + lambda_seg equilibrium-error budget (drift peaks ~0.05 at λ=0.5, global Q stays 0.5). **Phase 1**: full single-mass LIMEPY core — `limepy_density_hat`=E_γ(g+3/2,W) [index from App.B, NOT the misprinted main-text g+1/2; verified 3 ways incl. erratum], `solve_limepy_profile`, `LIMEPYProfile`, `LIMEPYVelocityDF`, all iso+aniso, validated ≡King(g1 iso)/≡Michie(g1 aniso) with Q=0.5 unscaled + jax.grad in (W0,g,r_a). KEY: jax `hyp1f1` is NaN for x≳100 → anisotropic density via an exact stable 1-D quadrature (bounded T(β) Poisson sum). Ingested LIMEPY paper+2018 erratum → bib + per-paper note. **Phase 2**: multi-mass coupled equilibrium (3-layer, docs/plans/2026-06-09-multimass-limepy-equilibrium-design.md): `solve_multimass_limepy` (α_j-direct core; δ=0≡single-mass oracle), `find_alpha_for_masses` (fixed-iter jax.lax.scan eigenvalue solve, √(M_j/M_j') update, residual<2e-3), `MultiMassLIMEPY` (from_alpha/from_imf + sample_cluster). Bug found+fixed: velocity scale uses Σm_i (actual sampled mass), not an independent M_total (was Q=0.93@δ=0). HEADLINE validated: per-component σ_1d(r) ≡ analytic equilibrium moment (<1%), per-group Q_j≈0.5 + global Q=0.5 across δ∈[0,0.6] (light/global exact; heavy 0.50→0.55 softening/N-limited, honest), segregation ratio 1.0→2.6. scripts/validate_multimass_equilibrium.py 4-panel fig PASS. Public API exports added. **NEXT: Phase 3 (blend-vs-equilibrium at matched Λ_MSR + docs: flip lowered-model-family.md planned→done, dashboard, mass-segregation.md §2 caveat→quantified).** Branch pushed per Anna's push-on-validation instruction; final PR deferred to end of arc.

PRIOR (still holds): **MERGED + PUSHED to main (2026-06-09, merge 23e5757): differentiable mass-segregation observables (milestone A).** Released-core `progenax.diagnostics.segregation_approx`: three differentiable segregation *observables* (soft Λ_MSR / radial concentration / Σ–m) sharing a sigmoid soft-mass-cut kernel w_i=σ((m_i−m_cut)/τ). Closes the gap the mass-segregation page flagged — a differentiable *observable* (f(positions,masses)→ℝ with usable ∂f), distinct from the already-differentiable forward-model *parameter* `lambda_seg`. TDD: +27 unit (tests/unit/diagnostics/) +14 validation (soft→exact hard-limit [radial/Σ to ~1e-11 via jnp.sort exact k-NN radius; λ-NN to 5.6e-3], monotonic+Spearman 0.98 vs Allison Λ_MSR oracle, regimes, AD-vs-FD ~1e-10, + delta-core limit). scripts/validate_segregation_approx.py 5 figs ALL PASS (layouts fixed for overlap) incl. **Fisher-information identifiability** (radial ~5× most identifiable, I=649 vs 132/143 → recommended HMC summary) + **2D-vs-3D projection** Fisher-ratio (radial keeps ~50%, Σ–m ~32%, local-NN λ unaffected). mass-segregation.md §5 expanded with per-figure physics interpretation+correctness verdicts; unicode→KaTeX. **Also rebuilt the §1 Λ_MSR regimes figure** on a shared realistic Plummer baseline after Anna's review (was an unphysical 1e-3 pc delta-core → scale-arbitrary Λ≈410 + per-panel cluster switching; estimator itself always correct). Released-core 893→935 (full suite, exit 0); myst build clean. Design: docs/plans/2026-06-09-differentiable-segregation-observable-design.md. **Deferred to milestone B: data-space inference, noisy mass proxy, learned 2D→3D deprojection mapping.** Prior campaign-complete state otherwise holds (gravoturb_fdf/pp20 still ⚠️ experimental).

SCIENTIFIC-CORRECTNESS PASS (2026-06-09, a9bf54a): line-by-line review of the full IMF/binary/environment code vs HELD PDFs (3 parallel audits + independent verification). KEY BUG: `alpha3_marks_plane` used the Marks+2012 PUBLISHED TYPO threshold +0.87 (a missing minus sign); the 2014 erratum (now held docs/core-papers/marks-2014-erratum.pdf) + Marks Fig.6 prove it's -0.87, and the authors used -0.87 (Table 1 unaffected). Fixed → corrected Marks ≡ Jeřábková IGIMF (gap 0.008, SAME relation; the earlier "two divergent models" Fig.4 was wrong). Also: differentiable.py 4-segment power_integral α=1 divide-by-zero (NaN likelihood+grad → added safe-denominator guard + TDD regression). Provenance lesson REPEATED: a review agent falsely said Salpeter/Kroupa/Maschberger PDFs not held → I propagated it into imf-statistics.md; they ARE held → corrected (all 4 IMF forms now PDF-verified). Chabrier mean 0.607 over [0.08,100] CONFIRMED correct (independent integral 0.604); the "~0.35" expectation is the mean integrated to m_min=0.01 (substellar included). Everything else PASSED (IMF forms+Jacobians, Kepler/orbit conservation, binary-aware 1/m1 Jacobian via integral=E[f_b], Moe Table 13, Marks Table 3/4 + 8π density).

CAMPAIGN R2/R3 (2026-06-09, pushed): **analytical-test-cases** (9b96bc5): scripts/validate_analytical.py — two-body Kepler (E=−Gm₁m₂/2a, closes 1.1e-7), Chenciner–Montgomery figure-eight (L=0 exact, closes 4.3e-8), Kepler III across 8 planets vs observed periods (max rel 6.9e-3) + IAU 2009 mass ratios, harmonic oscillator, adversarial spun-figure-8 (L=0.30 fails closure); fixed stale page (false "no test_analytical_physics.py", wrong Aarseth1974→Chenciner-Montgomery cite). **tidal-truncation** (2db8405, harden-then-validate): NEW tests/validation/test_tidal_physics.py (9 tests) validates r_J vs the restricted-3-body L1 point (Hill correction ∝(m/M_g)^(1/3), slope 0.34), the 3Ω² force balance, the (3/2)^(1/3) Keplerian-vs-isothermal factor, and apply_tidal_truncation vs analytic Plummer M(<r); scripts/validate_tidal.py 5 figs; differentiable in r_t via straight-through surrogate (vs analytic shell, median 0.02).

CAMPAIGN R2 EXTENSION (2026-06-08, pushed): 3 more modules to pub standard, each 5 build-verified figures + Measured-column tables. **imf-statistics** (c0b8cfe): scripts/validate_imfs.py rewrite onto _plotstyle — recovered slopes via sample-MLE (Salpeter 2.347±0.004, Kroupa 0.294/1.297/2.301), PDF/CDF coverage+KS, mean_mass() vs 200k log-grid ref (≤1.2e-4) with an adversarial linear-grid failure-mode panel, inference-layer gradient (rel 1e-5); fixed classic.md stale Chabrier params (0.22/0.57 were the *system* IMF → code defaults single-star 0.08/0.69), wrong class name, non-existent API kwargs. **binary-imf** (d43e0c5): scripts/validate_binaries.py — Kepler/orbit oracles (machine-precision), Moe+17 q-dist+KS (mass-dependent twin excess 15.2%→6.3%), HEADLINE regenerated "confidently wrong" via fast differentiable MLE (no MCMC): naive single-star fit 17.8σ-wrong at N=1e5 (α̂=2.21 vs 2.30) while binary-aware recovers 2.298 (0.4σ); bias∝f_b →0 at f_b=0; grad 8e-6; fixed stale binary_aware_validation.py script name. **environment-IMF** (9c358d9, harden-then-validate): NEW tests/validation/test_environment_physics.py (12 tests vs published Marks+2012 Table 1 GC α3 + Table 4 slopes) → released-core 866→878; scripts/validate_environment.py — GC anchors (NGC 7078 most top-heavy 0.844 vs pub 0.76), Fundamental Plane (density beats metallicity 7:1), low-mass slopes exact, HONEST Marks-vs-Jeřábková cross-model (both top-heavy mechanism but ~0.7 zero-point divergence reported not asserted away), grad 9e-8. All myst-build-verified (figs in _build/site/public + page JSON); new 50-validation/environment-imf.md wired into nav+dashboard+audit-report.

CAMPAIGN SUMMARY (2026-06-08): 6 modules at publication standard, each 5 build-verified figures + Measured-column tables: Plummer, King(retrofit), EFF, Michie, rotation/OM-anisotropy, substructure/CW04 Q; diagnostics (Λ_MSR + energy-sorted segregation) already done. Shared scripts/_plotstyle.py; LIMEPY reframed across 13 pages + roadmap (10-theory/spatial-profiles/lowered-model-family.md); validate_profiles.py deleted. Released-core 830→861 (+31 validation tests, all collect clean, build clean). Commits f964ebf, 4db6dbb, ee5f299, fcbfa32, 66cedaf on local main. Findings handled honestly: King W0 differentiable (corrected docs); EFF γ=5≡Plummer exact; Michie β suppressed below Osipkov-Merritt (validated vs DF moment oracle); OM stretch realizes β exactly (the Michie contrast); q_approx kNN-surrogate gradient (median 0.9%, cell-boundary worst-case). **rotation/anisotropy DONE (2026-06-08)**: new tests/validation/test_rotation_anisotropy_physics.py (10 tests, released-core 842→852) + scripts/validate_rotation_anisotropy.py (5 figs) + 50-validation/rotation-om-anisotropy.md. Solid-body v_φ=ΩR slope exact (resid 3e-14), L_z budget exact, differential curve <1e-6; OM anisotropy β(r)=r²/(r²+r_a²) realized EXACTLY for Plummer+EFF (dev 0.024/0.016) via Merritt-1985 direction-stretch — the clean CONTRAST to Michie's suppressed β. Ω/v_peak/r_a all differentiable (3e-9/7e-9/8e-6). **Michie DONE (2026-06-08)**: new tests/validation/test_michie_physics.py (12 tests, released-core 830→842) + scripts/validate_michie.py (5 figs) + 50-validation/michie-anisotropy.md. KEY FINDING: Michie-King β(r) is SUPPRESSED below the pure Osipkov-Merritt ceiling r²/(r²+r_a²) because the lowering term −exp(−J²/2r_a²σ²) breaks the f(Q) form — validated the sampler against the model's OWN DF β (2nd-moment oracle), not OM. γ/W0/r_c/M all differentiable. Per-module: status check → brainstorm plot set (HITL) → build pub figures → embed → BUILD-VERIFY → dashboard. **Plummer + King + EFF (2026-06-08) all done**: dedicated scripts/validate_{plummer,king,eff}.py on shared scripts/_plotstyle.py, each 5 pub figures printing expected-vs-measured; validation tables carry Measured columns; validate_profiles.py deleted (superseded). EFF: fixed false page claims (no γ>3 ValueError, no from_rh r_h-mapping, no KS test, "γ differentiable unlike King's W0" was wrong — both differentiable); γ=5→Plummer exact; γ AD-grad 2e-11. Entry point: docs/plans/2026-06-08-validation-audit-and-methods-figures-spec.md (per-module: brainstorm plot set → build pub-quality figures → embed on 50-validation page → BUILD-VERIFY render → update index dashboard). Done so far: **Plummer (2026-06-08, 5 pub figures)** + **King (32 tests, 5 figs)** + two-component + mass-segregation pages current+figure-bearing+build-verified; Λ_MSR validated; Tier C built. NEW shared scripts/_plotstyle.py (Okabe-Ito + ApJ rcParams); King refactored onto it. Plummer: dedicated scripts/validate_plummer.py (density+CDF, velocity-equilibrium, Beta(3/2,9/2), gradient AD-vs-FD, isotropy), each printing expected-vs-measured; Plummer+King validation tables now carry a Measured column (real regenerated numbers, fixed old overclaims). LIMEPY reframed across 13 pages (cite Gieles2015, own differentiable lowered-model family) + new roadmap 10-theory/spatial-profiles/lowered-model-family.md. EFF figure rewrite in validate_profiles.py still pending the EFF audit (validate_profiles.py is now Plummer-stale — its validate_plummer() superseded by scripts/validate_plummer.py; clean it during EFF).
blocker: **AWAITS ANNA — close-out of the gradient-gate lock-in arc.** CI minutes are exhausted, so there is NO final PR: the three CI layers are pytest tests verified locally (full released-core 1267 passed), and the `gradient-gate` CI job is wired/committed for when minutes return. Close out by **local merge of `feat/gradient-gate-lockin` → main on Anna's explicit go** (do NOT push/merge before). Phase C remaining: website Tier-5 + manifest-coverage section (`make build` 0 warnings), then the merge.
due: Anna's go (local merge).

## Current focus
_Seeded 2026-06-07 by the brain STATUS.md convention (`~/brain/work/meta/status-convention.md`). Update in your progenax session; the brain pulls `next:`/`blocker:`/`due:` via `federate.py`._

Differentiable ICs / population generation (IMF, binaries, cluster profiles). Feeds the Cottrell census (Aim 1) + the jaxstro methods paper. Mature, actively committed.

## Recent progress (2026-06-08)
- **King auto-domain + differentiability.** `from_W0_rc`/`KingVelocityDF` now
  auto-size the ODE domain from W0 (tracer-safe: falls back to fixed domain under
  jit/grad-over-W0, so differentiability is preserved) — high-W0 (to 15) models
  "just work"; backward-compatible for W0≤9. Released core 822→830. Added Fig 5
  gradient-validation (AD vs FD: r_c 2e-10, W0 2e-7, M 1.6e-6) and CORRECTED the
  docs: W0 IS differentiable (diffrax dψ/dW0); only the scalar r_t crossing is
  blocked (argmax). Joint (W0, r_c, M) gradient/HMC inference is supported.
- **profiles/King audit + release-hardening.** king-profile.md rewritten to the
  exact tested tolerances; 4 build-verified figures via scripts/validate_king.py
  (concentration vs Table II, density-vs-oracle [Option A: K-function overlay
  removed], velocity equilibrium, W₀ sweep); 50-validation index dashboard added.
  Release-grade stale-text sweep across all King files (king.py, king_df.py,
  test_king_physics.py, king.md + king-dfs.md theory, validate_profiles.py):
  removed internal audit labels (B2.0/B2.1/M5/M6/C2), bug-history comments, the
  uncited "W0>12 unstable" note, and the **fabricated** "validation cross-checks
  against LIMEPY at g=1" claim; fixed king-dfs.md sampling description to match the
  code (per-particle 1-D inverse-CDF via vmap, not a 2-D table). 24 King tests +
  822 released-core green; all 4 figures build-verified in _build + page JSON.
- **Segregation module validated in progenax (no gravax).** Λ_MSR diagnostic
  (`progenax.diagnostics.compute_lambda_msr`, Allison 2009) validated against analytic ground
  truth — `tests/validation/test_mass_segregation_physics.py` (8 tests: unsegregated→1, exact
  N=2, maximal→≫1, inverse→<1, convergence, binary caveat). Released-core **814→822**.
- **Paper grounding.** `docs/website/99-bibliography/per-paper/allison-2009.md` rewritten + verified
  vs the held ApJ 700 L99 PDF; found+fixed a docstring mis-citation (L99 Eq.1 = Spitzer t_seg, not
  Λ_MSR; formal def is MNRAS 395,1449 — PDF not held).
- **Validate-script cleanup (round 1).** 5 of 7 `scripts/validate_*.py` were broken (FDF clean-room +
  feature removal). Deleted 3 obsolete (`validate_fdf`, `validate_tail_sampling`,
  `validate_imf_extensions`); refactored `validate_mass_segregation` + `validate_cluster_ic` to the
  live API (all PASS). Commits 0745731, d327774.
- **Gravax dynamical mass-segregation experiment DEFERRED** (Anna's call) — full spec in
  `docs/notes/2026-06-08-gravax-segregation-validation-followup.md`.

## Open
- [x] FDF ICs: spherical envelope, `build_cluster_ic`, validation gallery, CW04 (m̄,s̄) plane
- [x] Λ_MSR segregation diagnostic validated (analytic Tier-A) + stale validate-script cleanup r1
- [ ] **validation/plots + experimental scratch-driver cleanup (round 2)** — retire banked-2D-β
      scratch (`_d0*`,`_v2*`,`_v3/4/5` + their plots); classify verified-vs-stale (in progress)
- [ ] integrate verified validation plots into the website docs pages
- [ ] Tier C: density-correlated mass placement (`correlated_mass_assignment`) + grounded
      mass-weighted substructure metric
- [x] **Differentiable King r_t — DONE (gradient-audit arc, PR #8)**: the silent-zero
      was found+fixed via unclamped-ψ interpolation across King/Michie/LIMEPY/
      multicomponent (the deferred Approach-B IFT proved unnecessary — the diffrax
      solve already carries dr_t/dW0 once the clamp is lifted before _find_tidal_radius).
- [ ] ~~**Differentiable King r_t (Approach B, IFT)**~~ — SUPERSEDED (see above);
      use case is concrete (headline: tidal-field/Jacobi coupling r_t≈r_J → infer
      Galactic potential/orbits from cluster limiting radii, wiring in
      progenax.tidal). Plan + science cases:
      docs/plans/2026-06-08-king-differentiable-tidal-radius-deferred.md
- [ ] methods-paper Figs 1–3 (buildable now) + (deferred) gravax Figs 4–5
- [ ] (optional) m̄↔β quantitative calibration table

## Nice-to-do / revisit later
- Filamentary (non-Gaussian-phase) morphology for the FDF field — deferred (second-order for the
  N-body/binary purpose at matched P(k)+clumpiness+virial). Why-wait + pedagogical implementation
  (turbulent-shock / Zel'dovich displacement + copula; equations) in
  docs/plans/2026-06-07-gravoturb-fdf-spherical-ic-design.md (§ NICE-TO-DO). Revisit only if a
  dynamical observable distinguishes filament-vs-blob ICs, or for direct morphology/gas→star studies.

## Decided (2026-06-07)
- 2D-projected β inference from star catalogues: **banked as a methods result, not optimizing
  further** (cosmic-variance + dynamics limited; gas measures β more directly). Analytic high-N
  SBC p=0.82, flowjax NPE low-N p=0.20. Pivot to FDF ICs as a forward generative tool + (future)
  3D Gaia inference where the projection wall vanishes. Captured to brain inbox 2026-06-07.
