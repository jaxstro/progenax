# progenax — status

next: **Batch B — cross-engine validation figures + science demos (separate brainstorm + plan).** **DONE (2026-06-10, Anna-approved): DF sampler-fusion micro-batch** (profile-driven: staged RSS decomposition showed construction ~1.7 GB dominates, eager draw +0.63 GB): speed-CDF tables cached at construction + jitted sampling cores (eqx.filter_jit) for LIMEPY/King/Michie standalone DFs — warm aniso draws **0.35 s → 15 ms** at N=2e4 (3–16 ms across N; 55× vs pre-batch quadrature at N=1e4), draw-chain peak +0.63→+0.20 GB, stage 2.37→2.02 GB, `limepy_df_aniso` gate tightened 3.0→2.5 GB, small-N build penalty eliminated; lax.map chunking of the table draw evaluated and rejected on measurement. Fidelity re-verified on the jitted path (identical KS/moment/β, ALL PASS). Gate: **1163** released-core (unit 895; fast 1121). Remaining DF-stage floor is model CONSTRUCTION (~1.8 GB, paid once per model) — future target if ever needed. **DONE (2026-06-10, Anna-approved): Batch A merged to local main** (fast-forward to `2263948`; FAST gate re-verified on merged main 1108; main NOT pushed — 22+ ahead of origin, awaits Anna's go) **+ the performance-evidence addendum** (Anna-requested, plan-approved): `scripts/benchmark_batch_a.py` measures BOTH code versions live (pre-batch worktree at `0dd1cd9` vs HEAD) — wall-clock: PE 2.4× faster at N=8k (0.169→0.070 s), N=2e4 feasible at 0.216 s (dense: 20.9 GB, infeasible), honest small-N regressions measured (PE 3.5× slower at N=2k; DF table-build dominates below ~5k); memory: 16× at N=8k (gated ≥5×); fidelity figures table-vs-quadrature ALL PASS (KS ≤ 6.7e-3); NEW `50-validation/performance-memory.md` page (myst 144 pages, 0 warnings) + King/Michie fidelity embeds. The multi-component cluster arc is DONE end-to-end: Engines A (LIMEPY DF tables) + B (density-defined Eddington) merged to main (`4f4f259`), the post-merge code-review fix batch landed (`0dd1cd9`), and **Batch A (memory + consolidation + docs)** is complete — blocked+remat'd virial kernels (forward AND grad O(block·N)), table-routed standalone LIMEPY/King/Michie DFs (quadrature kept as the `speed_method="quadrature"` oracle), `progenax.numerics` consolidation (cumtrap ×5 + inverse-CDF draws), `_EngineAState` grouping (A-only access on B raises informative AttributeError), shared angular conditional in `kinematics/_speed_kernels.py`, `lax.map(batch_size=2048)` bounded oracles, and the docs metrics refresh. **Memory headline (measured, `scripts/profile_cluster_memory.py`, all PASS): virial kernel at N=2e4 32.8 GB → 0.41 GB; standalone anisotropic DF sampling 10.9 GB → 2.3 GB; Engine A/B builds at N=1e5 ≤ 2.5 GB.** Gate: released-core **1150 passed** (unit 882 / integration 34 / validation 234; fast gate 1108). **Merge/push of `fix/memory-and-consolidation` awaits Anna's go.**

PRIOR (collapsed; superseded-arc detail in `docs/plans/` + `.claude-work/`): **UNIFIED MULTI-COMPONENT CLUSTER REDESIGN on branch `feat/multimass-limepy-equilibrium`.** An 8-dimension adversarial review of the two-component/segregation code found ONE real physics bug (`generate_two_component_cluster` feeds the FULL mass to each sub-population's velocity DF + no COM/virial rescale → non-self-consistent for unequal-mass IMFs; silent because tests use `jnp.ones(N)`) + an ad-hoc API; the `lambda_seg` blend + `energy_sorted_segregation` are sound. **Decision (Anna, pre-launch, no backwards-compat):** build ONE unified differentiable `MultiComponentCluster` (eqx.Module → ICResult) spanning mass-seg · GC 1G/2G · binaries-vs-singles · halo+core, via TWO engines — **Engine A** (DF-defined lowered-isothermal/LIMEPY, mostly built) + **Engine B** (density-defined Eddington/OM for Plummer/EFF/King). Keep `energy_sorted_segregation` as a labeled PRIMORDIAL generator; **retire the `lambda_seg` blend**. Subsume+retire `MultiMassLIMEPY` (mass-seg → a `.from_mass_segregation()` constructor). Plan: `~/.claude/plans/we-are-continuing-progenax-s-crispy-rain.md`. KEY INSIGHT: a component's single free Engine-A scale is the velocity-scale ratio `w_j=s_j/s` with `rescale_j=w_j^-2`; mass-seg is `w_j=μ_j^-δ`. **DONE: Phase 1a** — extracted `solve_multicomponent_limepy(alpha_j, rescale_j, W0, g, ra_hat_j=None)` core (direct per-component scales); `solve_multimass_limepy` is now a thin mass-wrapper (bit-identical, verified); +5 TDD unit tests; committed `47dc24a`. **DONE: Phase 1b (commit `13de746`, NOT pushed)** — `progenax.cluster.multicomponent.MultiComponentCluster` (eqx.Module): physics flows from `(w_j, rescale_j=w_j^-2, ra_hat_j)`, `m_j` decoupled (labels only); `.from_components` (general: GC 1G/2G, halo+core) / `.from_mass_segregation` (w_j=μ_j^-δ) / `.from_imf`; `sample_cluster → ICResult` with NEW optional `component_id` field; the NEW unequal-mass two-population regression passes (theory Q_j=0.5±2e-3, sampled global ±0.04, per-component ±0.07); `MultiMassLIMEPY` RETIRED (tests ported, validation file + both `validate_multimass_*` scripts rewired and re-run PASS, exports updated). Released-core **1065 passed, exit 0** (1056 at 1a − 14 ported-out + 23 new). **DONE: speed fixes** (commit `3f15479`): pytest-xdist + XLA thread caps + `slow` markers (FAST/FULL gates in CLAUDE.md); solver `rho_j_grid` reuse; jitted sampler core. **DONE: Phase 1.5 — LIMEPY DF tables** (Anna-approved plan `docs/plans/2026-06-09-limepy-df-tables-phase15.md`, subagent-driven with per-task independent review; commits `dda2887..f69f36d` + close-out, NOT pushed): three differentiable table primitives in `profiles/limepy_tables.py` (`AnisoDensityTable` (√W, asinh p) cubic-Lagrange; `SpeedCDFTable` 256×256; `AnisoSpeedCDFTable` 192×48×192 speed marginal with EXACT angular conditional) replace pointwise quadrature in the coupled-Poisson RHS + both sampler branches. **Measured: aniso construction 957→170 ms (5.6×); sampling at N=1e5: iso 67×, aniso 21.7× (0.48/2.9 µs/star); aniso small-N break-even ~3k stars (documented).** Every approximation budget-asserted with the exact quadrature retained as a selectable oracle (`aniso_method="quadrature"`) — density 6.05e-6 (1e-5 budget), solve |Δψ|≤1.93e-4 over 3 configs, CDF 4.5e-5, speed moments ≤0.28%/1.5% vs DF quadrature, β(r)≡DF <0.06 unchanged, AD-vs-FD 2.15e-4 + table-AD≡quadrature-AD 2.1e-4; `component_virial_ratios` deliberately quadrature-ONLY (oracle independence: table-built model proves Q_j=0.5001±1.5e-4). Reviews caught+fixed 3 real bugs pre-merge (W≤0 NaN gradient at the truncation boundary; high-g (≥2.5) CDF-row underflow corrupting outer-shell speeds, now gated g∈{0,2.5,3.5}; the plan's own √(2π) test bug). `scripts/validate_df_tables.py` ALL PASS; completion doc `.claude-work/TASK_1.5_DF_TABLES_COMPLETE.md`. Released-core at 1.5 close-out: 1098 (gate evidence in `.claude-work/TASK_1.5_DF_TABLES_COMPLETE.md`, commit `a85bada`). **DONE: Phase 1c (commits `b1caabb`, `8cda23d`, + 1c-3; NOT pushed)** — the Anna-approved OUTRIGHT DELETION (−3.4k LOC): `populations.py` (the full-mass-to-each-DF physics-bug module) + `cluster/core.py` (string-dispatch `generate_cluster_ic`/`ClusterState`/`SpatialStructureParams`/`MassSegregationLayer`) + `cluster/validation.py` + all `lambda_seg` machinery/scripts RETIRED; `test_segregation_equilibrium_physics.py` PORTED onto the primordial generator via the protocol API (λ=1 endpoint Q_j drift<0.08, orbit-reuse regression, Λ_MSR rises); `validate_cluster_ic.py` §2 rewired to `MultiComponentCluster` (cold w=0.7 median r 1.50 pc < hot 8.36 pc, oracle Q_j=[0.5,0.5], ALL PASS); `mass_segregation.py` relabeled PRIMORDIAL (documented departure from Baumgardt+2008 random per-bin draw; wrong MNRAS 384,1231 cite died with populations.py; dead `_mcluster_partial_shuffle` removed); **eqx migration complete — src/ is dataclass-free** (`ICResult`/`Systems`/`Stars`/`TotalMass`/`AnalyticalIC`/`RotationParams`/`VelocityModel` now eqx.Modules; physical scalars stay differentiable leaves, control-flow config static; jit-over-ICResult verified); `compute_profile_potential` docstring fixed (SPECIFIC potential, [length]²/[time]²). Full gate **1064 passed in 438s** (unit 802/integration 34/validation 228; fast gate 1029; exact count reconciliation 1098−28−7−4+4+1). Phase-3 note: 16 docs/website source pages still reference the deleted API (inventoried; the docs sweep is Phase 3 scope). **DONE: Phase 2 — Engine B (density-defined Eddington), commits `292b6b0..08e328b` + close-out, NOT pushed:** `MultiComponentCluster.from_density_profiles(profiles, mass_fractions, m_j, r_a_j=None, r_t=None)` — Plummer/EFF/King DENSITY components in ONE shared self-consistent potential (no ODE: one cumtrap Poisson pass, new `profiles/density_poisson.py` with derived-domain provenance + King-override conflict rule), per-component DFs via the generic `eddington_invert` (extracted from the EFF table BIT-IDENTICALLY, pinned; + Plummer E^{7/2} (untruncated zero point) and EXACT truncated closed-form oracles, 1.06e-4/8.86e-6), optional per-component OM `r_a_j`, f_j≥0 realizability gate (concrete builds refuse naming the component + f_min + remedy; traced builds store the `f_min_j` diagnostic). Anchors ALL PASS (`scripts/validate_multicomponent_eddington.py`, 11/11): King A-vs-B (TWO independent engines: σ_1d(r) dev 3e-4, radial KS 2e-4, both gates 0.02), EFF(γ=5)≡Plummer twins, halo+core headline (theory Q_j=0.5±4e-4; sampled global Q=0.4976 UNSCALED at N=30k; sampled per-component Q_j ≡ the hybrid predict-the-offset quadrature expectation — the hard-truncated halo's plateau 0.4953 predicted / 0.4947±0.0014 sampled (18 seeds) is VERIFIED truncation-edge physics, gated on the prediction, Anna-approved), OM β_halo(r)≡r²/(r²+r_a²) (max dev 0.028 < 0.05), DF-density fidelity 1.06e-3 (OM) / 2.4e-4 (iso) vs 5e-3 gate, AD≡FD 5.6e-9/7.8e-7/2.0e-8 (r_h, mass-fraction t, r_a). THREE physics findings: (1) the E^{7/2} oracle needs the UNTRUNCATED zero point (truncation shifts E by c=M/√(r_t²+a²), ~30% at 0.1Ψ₀); (2) the plan's default a_EFF=0.4 core is GENUINELY unrealizable (f_min −0.20; gate flip measured a∈(0.65,0.68); headline uses 0.8); (3) hybrid-sampling Q_j plateau predicted+verified. Plus the King dW/dr staircase bug (jnp.gradient of interpolated ψ → Eddington ringing, f_min −0.679) fixed via King's own Poisson identity → +5.1e-7. Full gate **1101 passed in 511s, exit 0** (unit 833, integration 34, validation 234; fast gate 1059); close-out `.claude-work/PHASE_2_ENGINE_B_COMPLETE.md`. **DONE: merge + push (2026-06-10, Anna-approved):** `feat/multimass-limepy-equilibrium` fast-forwarded into `main` (`18f116b..4f4f259`), merged-main gate re-verified **1101 passed**, pushed; local branch deleted (stale remote branch copy left — remote deletion not authorized). **DONE: Phase 3 docs sweep (2026-06-10):** comprehensive website update — `lowered-model-family.md` flipped roadmap→implemented Engine A methods page; populations section rewritten around the two-engine `MultiComponentCluster` (+ NEW `10-theory/populations/eddington-engine.md`); API reference regenerated (PUBLIC_MODULES −populations +cluster/diagnostics/dynamics; 11 module pages, 175 symbols; `30-api/populations.md` deleted); NEW `50-validation/{multimass-equilibrium,engine-b-eddington}.md` with Measured tables + figures (stale mass-seg numbers corrected by re-running validate_cluster_ic.py: Λ_MSR 4.74, ρ(m,E)=−0.92); all 16 inventoried stale-API pages fixed (dev-log: dated retirement admonitions, history preserved); myst build clean (142 pages, 0 warnings). **DONE: /code-review of the merged arc (7 finder angles + verification):** 3 CONFIRMED post-merge correctness bugs — (1) NaN-blind realizability gate (`nan < -1e-3` False; r_a_j=0 → silent all-NaN model), (2) no mass_fractions/m_j/r_a_j length validation vs profiles (phantom components via clamped gathers; potential built from wrong total mass), (3) absolute Ψ thresholds in `sample_speed_from_f_table` are scale-dependent (r_h≳1e4 pc → exact-zero speeds; 80% of stars at r_h=1e6) — plus `total_density()`/`rescale_j` silent-NaN on Engine B, `is_aniso=False` lie for OM-B models, `r_t_provenance` static field embedding float values (per-r_t recompile + vmap break), and a consolidation list (DF speed-moment kernel ×5 copies w/ inconsistent n_w, cumtrap Poisson pass ×3, King density/derivative split, union-by-NaN → _EngineAState grouping). All documented honestly in the new docs limitations admonitions. **RESUME AT: the code-review fix batch (findings 1–3 first; then 4–6; consolidations after), then cross-engine validation figures + science demos.** HITL: push only when Anna says.

PRIOR next (superseded by the redesign above): **LIMEPY multi-mass equilibrium arc — Phases 0–2b + anisotropic IC sampler DONE. Released-core 861→1051 (verified `1051 passed`, exit 0). Pushed.** **Phase 2b (per-component anisotropy η)**: `solve_multimass_limepy`/`MultiMassLIMEPY` take `ra_hat`,`eta` → per-component r_{a,j}=r_a·μ_j^η; aniso density via the stable T(β) quadrature; theoretical Q_j=0.5 holds with the anisotropic v² moment; differentiable in (r_a,η,δ). **Anisotropic IC sampler**: `sample_cluster` aniso branch reuses the validated `_sample_speed_angle` Michie/OM kernel (per-star W_i=rescale_i·ψ, p_i=(r/r_c)/(r̂_a·μ_j^η), v=s_i·(u_r r̂+u_t t̂)). Validated: global Q=0.5 across δ (scalar virial is anisotropy-blind); sampled β_j(r) ≡ DF's own (u,c)-quadrature β across resolved bins (rise to ~0.22 peak near 0.5r_t + truncation TURNOVER — the LIMEPY signature); edge β is finite-N ratio-noise (⟨v_r²⟩→0), shown converging to DF under seed-averaging (16-seed: 0.65–0.75r_t→0.191±0.012 vs DF 0.193). +5 unit + 1 validation test; scripts/validate_multimass_anisotropy.py (seed-averaged β±sem) PASS. Partial-equilibrium hardening of mass segregation: a first-principles, differentiable, multi-mass lowered-isothermal family (Gieles & Zocchi 2015) that COMPLEMENTS the `lambda_seg` blend. **Phase 0**: per-mass-group virial diagnostic `per_group_virial_ratio` (dynamics/virial.py) + FIXED a released bug (`energy_sorted_segregation` orbit-reuse → coincident stars/V=−∞ for steep IMFs at λ=1; deterministic monotonic assignment) + lambda_seg equilibrium-error budget (drift peaks ~0.05 at λ=0.5, global Q stays 0.5). **Phase 1**: full single-mass LIMEPY core — `limepy_density_hat`=E_γ(g+3/2,W) [index from App.B, NOT the misprinted main-text g+1/2; verified 3 ways incl. erratum], `solve_limepy_profile`, `LIMEPYProfile`, `LIMEPYVelocityDF`, all iso+aniso, validated ≡King(g1 iso)/≡Michie(g1 aniso) with Q=0.5 unscaled + jax.grad in (W0,g,r_a). KEY: jax `hyp1f1` is NaN for x≳100 → anisotropic density via an exact stable 1-D quadrature (bounded T(β) Poisson sum). Ingested LIMEPY paper+2018 erratum → bib + per-paper note. **Phase 2**: multi-mass coupled equilibrium (3-layer, docs/plans/2026-06-09-multimass-limepy-equilibrium-design.md): `solve_multimass_limepy` (α_j-direct core; δ=0≡single-mass oracle), `find_alpha_for_masses` (fixed-iter jax.lax.scan eigenvalue solve, √(M_j/M_j') update, residual<2e-3), `MultiMassLIMEPY` (from_alpha/from_imf + sample_cluster). Bug found+fixed: velocity scale uses Σm_i (actual sampled mass), not an independent M_total (was Q=0.93@δ=0). HEADLINE validated: per-component σ_1d(r) ≡ analytic equilibrium moment (<1%), per-group Q_j≈0.5 + global Q=0.5 across δ∈[0,0.6] (light/global exact; heavy 0.50→0.55 softening/N-limited, honest), segregation ratio 1.0→2.6. scripts/validate_multimass_equilibrium.py 4-panel fig PASS. Public API exports added. **NEXT: Phase 3 (blend-vs-equilibrium at matched Λ_MSR + docs: flip lowered-model-family.md planned→done, dashboard, mass-segregation.md §2 caveat→quantified).** Branch pushed per Anna's push-on-validation instruction; final PR deferred to end of arc.

PRIOR (still holds): **MERGED + PUSHED to main (2026-06-09, merge 23e5757): differentiable mass-segregation observables (milestone A).** Released-core `progenax.diagnostics.segregation_approx`: three differentiable segregation *observables* (soft Λ_MSR / radial concentration / Σ–m) sharing a sigmoid soft-mass-cut kernel w_i=σ((m_i−m_cut)/τ). Closes the gap the mass-segregation page flagged — a differentiable *observable* (f(positions,masses)→ℝ with usable ∂f), distinct from the already-differentiable forward-model *parameter* `lambda_seg`. TDD: +27 unit (tests/unit/diagnostics/) +14 validation (soft→exact hard-limit [radial/Σ to ~1e-11 via jnp.sort exact k-NN radius; λ-NN to 5.6e-3], monotonic+Spearman 0.98 vs Allison Λ_MSR oracle, regimes, AD-vs-FD ~1e-10, + delta-core limit). scripts/validate_segregation_approx.py 5 figs ALL PASS (layouts fixed for overlap) incl. **Fisher-information identifiability** (radial ~5× most identifiable, I=649 vs 132/143 → recommended HMC summary) + **2D-vs-3D projection** Fisher-ratio (radial keeps ~50%, Σ–m ~32%, local-NN λ unaffected). mass-segregation.md §5 expanded with per-figure physics interpretation+correctness verdicts; unicode→KaTeX. **Also rebuilt the §1 Λ_MSR regimes figure** on a shared realistic Plummer baseline after Anna's review (was an unphysical 1e-3 pc delta-core → scale-arbitrary Λ≈410 + per-panel cluster switching; estimator itself always correct). Released-core 893→935 (full suite, exit 0); myst build clean. Design: docs/plans/2026-06-09-differentiable-segregation-observable-design.md. **Deferred to milestone B: data-space inference, noisy mass proxy, learned 2D→3D deprojection mapping.** Prior campaign-complete state otherwise holds (gravoturb_fdf/pp20 still ⚠️ experimental).

SCIENTIFIC-CORRECTNESS PASS (2026-06-09, a9bf54a): line-by-line review of the full IMF/binary/environment code vs HELD PDFs (3 parallel audits + independent verification). KEY BUG: `alpha3_marks_plane` used the Marks+2012 PUBLISHED TYPO threshold +0.87 (a missing minus sign); the 2014 erratum (now held docs/core-papers/marks-2014-erratum.pdf) + Marks Fig.6 prove it's -0.87, and the authors used -0.87 (Table 1 unaffected). Fixed → corrected Marks ≡ Jeřábková IGIMF (gap 0.008, SAME relation; the earlier "two divergent models" Fig.4 was wrong). Also: differentiable.py 4-segment power_integral α=1 divide-by-zero (NaN likelihood+grad → added safe-denominator guard + TDD regression). Provenance lesson REPEATED: a review agent falsely said Salpeter/Kroupa/Maschberger PDFs not held → I propagated it into imf-statistics.md; they ARE held → corrected (all 4 IMF forms now PDF-verified). Chabrier mean 0.607 over [0.08,100] CONFIRMED correct (independent integral 0.604); the "~0.35" expectation is the mean integrated to m_min=0.01 (substellar included). Everything else PASSED (IMF forms+Jacobians, Kepler/orbit conservation, binary-aware 1/m1 Jacobian via integral=E[f_b], Moe Table 13, Marks Table 3/4 + 8π density).

CAMPAIGN R2/R3 (2026-06-09, pushed): **analytical-test-cases** (9b96bc5): scripts/validate_analytical.py — two-body Kepler (E=−Gm₁m₂/2a, closes 1.1e-7), Chenciner–Montgomery figure-eight (L=0 exact, closes 4.3e-8), Kepler III across 8 planets vs observed periods (max rel 6.9e-3) + IAU 2009 mass ratios, harmonic oscillator, adversarial spun-figure-8 (L=0.30 fails closure); fixed stale page (false "no test_analytical_physics.py", wrong Aarseth1974→Chenciner-Montgomery cite). **tidal-truncation** (2db8405, harden-then-validate): NEW tests/validation/test_tidal_physics.py (9 tests) validates r_J vs the restricted-3-body L1 point (Hill correction ∝(m/M_g)^(1/3), slope 0.34), the 3Ω² force balance, the (3/2)^(1/3) Keplerian-vs-isothermal factor, and apply_tidal_truncation vs analytic Plummer M(<r); scripts/validate_tidal.py 5 figs; differentiable in r_t via straight-through surrogate (vs analytic shell, median 0.02).

CAMPAIGN R2 EXTENSION (2026-06-08, pushed): 3 more modules to pub standard, each 5 build-verified figures + Measured-column tables. **imf-statistics** (c0b8cfe): scripts/validate_imfs.py rewrite onto _plotstyle — recovered slopes via sample-MLE (Salpeter 2.347±0.004, Kroupa 0.294/1.297/2.301), PDF/CDF coverage+KS, mean_mass() vs 200k log-grid ref (≤1.2e-4) with an adversarial linear-grid failure-mode panel, inference-layer gradient (rel 1e-5); fixed classic.md stale Chabrier params (0.22/0.57 were the *system* IMF → code defaults single-star 0.08/0.69), wrong class name, non-existent API kwargs. **binary-imf** (d43e0c5): scripts/validate_binaries.py — Kepler/orbit oracles (machine-precision), Moe+17 q-dist+KS (mass-dependent twin excess 15.2%→6.3%), HEADLINE regenerated "confidently wrong" via fast differentiable MLE (no MCMC): naive single-star fit 17.8σ-wrong at N=1e5 (α̂=2.21 vs 2.30) while binary-aware recovers 2.298 (0.4σ); bias∝f_b →0 at f_b=0; grad 8e-6; fixed stale binary_aware_validation.py script name. **environment-IMF** (9c358d9, harden-then-validate): NEW tests/validation/test_environment_physics.py (12 tests vs published Marks+2012 Table 1 GC α3 + Table 4 slopes) → released-core 866→878; scripts/validate_environment.py — GC anchors (NGC 7078 most top-heavy 0.844 vs pub 0.76), Fundamental Plane (density beats metallicity 7:1), low-mass slopes exact, HONEST Marks-vs-Jeřábková cross-model (both top-heavy mechanism but ~0.7 zero-point divergence reported not asserted away), grad 9e-8. All myst-build-verified (figs in _build/site/public + page JSON); new 50-validation/environment-imf.md wired into nav+dashboard+audit-report.

CAMPAIGN SUMMARY (2026-06-08): 6 modules at publication standard, each 5 build-verified figures + Measured-column tables: Plummer, King(retrofit), EFF, Michie, rotation/OM-anisotropy, substructure/CW04 Q; diagnostics (Λ_MSR + energy-sorted segregation) already done. Shared scripts/_plotstyle.py; LIMEPY reframed across 13 pages + roadmap (10-theory/spatial-profiles/lowered-model-family.md); validate_profiles.py deleted. Released-core 830→861 (+31 validation tests, all collect clean, build clean). Commits f964ebf, 4db6dbb, ee5f299, fcbfa32, 66cedaf on local main. Findings handled honestly: King W0 differentiable (corrected docs); EFF γ=5≡Plummer exact; Michie β suppressed below Osipkov-Merritt (validated vs DF moment oracle); OM stretch realizes β exactly (the Michie contrast); q_approx kNN-surrogate gradient (median 0.9%, cell-boundary worst-case). **rotation/anisotropy DONE (2026-06-08)**: new tests/validation/test_rotation_anisotropy_physics.py (10 tests, released-core 842→852) + scripts/validate_rotation_anisotropy.py (5 figs) + 50-validation/rotation-om-anisotropy.md. Solid-body v_φ=ΩR slope exact (resid 3e-14), L_z budget exact, differential curve <1e-6; OM anisotropy β(r)=r²/(r²+r_a²) realized EXACTLY for Plummer+EFF (dev 0.024/0.016) via Merritt-1985 direction-stretch — the clean CONTRAST to Michie's suppressed β. Ω/v_peak/r_a all differentiable (3e-9/7e-9/8e-6). **Michie DONE (2026-06-08)**: new tests/validation/test_michie_physics.py (12 tests, released-core 830→842) + scripts/validate_michie.py (5 figs) + 50-validation/michie-anisotropy.md. KEY FINDING: Michie-King β(r) is SUPPRESSED below the pure Osipkov-Merritt ceiling r²/(r²+r_a²) because the lowering term −exp(−J²/2r_a²σ²) breaks the f(Q) form — validated the sampler against the model's OWN DF β (2nd-moment oracle), not OM. γ/W0/r_c/M all differentiable. Per-module: status check → brainstorm plot set (HITL) → build pub figures → embed → BUILD-VERIFY → dashboard. **Plummer + King + EFF (2026-06-08) all done**: dedicated scripts/validate_{plummer,king,eff}.py on shared scripts/_plotstyle.py, each 5 pub figures printing expected-vs-measured; validation tables carry Measured columns; validate_profiles.py deleted (superseded). EFF: fixed false page claims (no γ>3 ValueError, no from_rh r_h-mapping, no KS test, "γ differentiable unlike King's W0" was wrong — both differentiable); γ=5→Plummer exact; γ AD-grad 2e-11. Entry point: docs/plans/2026-06-08-validation-audit-and-methods-figures-spec.md (per-module: brainstorm plot set → build pub-quality figures → embed on 50-validation page → BUILD-VERIFY render → update index dashboard). Done so far: **Plummer (2026-06-08, 5 pub figures)** + **King (32 tests, 5 figs)** + two-component + mass-segregation pages current+figure-bearing+build-verified; Λ_MSR validated; Tier C built. NEW shared scripts/_plotstyle.py (Okabe-Ito + ApJ rcParams); King refactored onto it. Plummer: dedicated scripts/validate_plummer.py (density+CDF, velocity-equilibrium, Beta(3/2,9/2), gradient AD-vs-FD, isotropy), each printing expected-vs-measured; Plummer+King validation tables now carry a Measured column (real regenerated numbers, fixed old overclaims). LIMEPY reframed across 13 pages (cite Gieles2015, own differentiable lowered-model family) + new roadmap 10-theory/spatial-profiles/lowered-model-family.md. EFF figure rewrite in validate_profiles.py still pending the EFF audit (validate_profiles.py is now Plummer-stale — its validate_plummer() superseded by scripts/validate_plummer.py; clean it during EFF).
blocker: none
due:

## Current focus
_Seeded 2026-06-07 by the brain STATUS.md convention (`~/brain/work/meta/status-convention.md`). Update in your progenax session; the brain pulls `next:`/`blocker:`/`due:` via `federate.py`._

Differentiable ICs / population generation (IMF, binaries, cluster profiles). Feeds the Cottrell census (Aim 1) + the jaxstro methods paper. Mature, actively committed.

## Recent progress (2026-06-08)
- **King auto-domain + differentiability.** `from_W0_rc`/`KingVelocityDF` now
  auto-size the ODE domain from W0 (tracer-safe: falls back to fixed domain under
  jit/grad-over-W0, so differentiability is preserved) — high-W0 (to 15) models
  "just work"; backward-compatible for W0≤9. Released core 822→830. Added Fig 5
  gradient-validation (AD vs FD: r_c 2e-10, W0 2e-7, M 1.6e-6) and CORRECTED the
  docs: W0 IS differentiable (diffrax dψ/dW0); only the scalar r_t crossing is
  blocked (argmax). Joint (W0, r_c, M) gradient/HMC inference is supported.
- **profiles/King audit + release-hardening.** king-profile.md rewritten to the
  exact tested tolerances; 4 build-verified figures via scripts/validate_king.py
  (concentration vs Table II, density-vs-oracle [Option A: K-function overlay
  removed], velocity equilibrium, W₀ sweep); 50-validation index dashboard added.
  Release-grade stale-text sweep across all King files (king.py, king_df.py,
  test_king_physics.py, king.md + king-dfs.md theory, validate_profiles.py):
  removed internal audit labels (B2.0/B2.1/M5/M6/C2), bug-history comments, the
  uncited "W0>12 unstable" note, and the **fabricated** "validation cross-checks
  against LIMEPY at g=1" claim; fixed king-dfs.md sampling description to match the
  code (per-particle 1-D inverse-CDF via vmap, not a 2-D table). 24 King tests +
  822 released-core green; all 4 figures build-verified in _build + page JSON.
- **Segregation module validated in progenax (no gravax).** Λ_MSR diagnostic
  (`progenax.diagnostics.compute_lambda_msr`, Allison 2009) validated against analytic ground
  truth — `tests/validation/test_mass_segregation_physics.py` (8 tests: unsegregated→1, exact
  N=2, maximal→≫1, inverse→<1, convergence, binary caveat). Released-core **814→822**.
- **Paper grounding.** `docs/website/99-bibliography/per-paper/allison-2009.md` rewritten + verified
  vs the held ApJ 700 L99 PDF; found+fixed a docstring mis-citation (L99 Eq.1 = Spitzer t_seg, not
  Λ_MSR; formal def is MNRAS 395,1449 — PDF not held).
- **Validate-script cleanup (round 1).** 5 of 7 `scripts/validate_*.py` were broken (FDF clean-room +
  feature removal). Deleted 3 obsolete (`validate_fdf`, `validate_tail_sampling`,
  `validate_imf_extensions`); refactored `validate_mass_segregation` + `validate_cluster_ic` to the
  live API (all PASS). Commits 0745731, d327774.
- **Gravax dynamical mass-segregation experiment DEFERRED** (Anna's call) — full spec in
  `docs/notes/2026-06-08-gravax-segregation-validation-followup.md`.

## Open
- [x] FDF ICs: spherical envelope, `build_cluster_ic`, validation gallery, CW04 (m̄,s̄) plane
- [x] Λ_MSR segregation diagnostic validated (analytic Tier-A) + stale validate-script cleanup r1
- [ ] **validation/plots + experimental scratch-driver cleanup (round 2)** — retire banked-2D-β
      scratch (`_d0*`,`_v2*`,`_v3/4/5` + their plots); classify verified-vs-stale (in progress)
- [ ] integrate verified validation plots into the website docs pages
- [ ] Tier C: density-correlated mass placement (`correlated_mass_assignment`) + grounded
      mass-weighted substructure metric
- [ ] **Differentiable King r_t (Approach B, IFT)** — DEFERRED until a scalar-r_t
      use case is concrete (headline: tidal-field/Jacobi coupling r_t≈r_J → infer
      Galactic potential/orbits from cluster limiting radii, wiring in
      progenax.tidal). Plan + science cases:
      docs/plans/2026-06-08-king-differentiable-tidal-radius-deferred.md
- [ ] methods-paper Figs 1–3 (buildable now) + (deferred) gravax Figs 4–5
- [ ] (optional) m̄↔β quantitative calibration table

## Nice-to-do / revisit later
- Filamentary (non-Gaussian-phase) morphology for the FDF field — deferred (second-order for the
  N-body/binary purpose at matched P(k)+clumpiness+virial). Why-wait + pedagogical implementation
  (turbulent-shock / Zel'dovich displacement + copula; equations) in
  docs/plans/2026-06-07-gravoturb-fdf-spherical-ic-design.md (§ NICE-TO-DO). Revisit only if a
  dynamical observable distinguishes filament-vs-blob ICs, or for direct morphology/gas→star studies.

## Decided (2026-06-07)
- 2D-projected β inference from star catalogues: **banked as a methods result, not optimizing
  further** (cosmic-variance + dynamics limited; gas measures β more directly). Analytic high-N
  SBC p=0.82, flowjax NPE low-N p=0.20. Pivot to FDF ICs as a forward generative tool + (future)
  3D Gaia inference where the projection wall vanishes. Captured to brain inbox 2026-06-07.
