---
title: Mass segregation validation
description: Current validation status for the Baumgardt-style mass-segregation layer and Λ_MSR diagnostics.
---
# Mass segregation validation

```{important}
Status: **unit-backed, not a dedicated validation suite yet**.

There is no `tests/validation/test_mass_segregation.py` file in this
checkout. The current implemented behavior is covered by
`tests/unit/cluster/test_cluster_ic.py`,
`tests/unit/cluster/test_mass_segregation.py`, and
`tests/unit/cluster/test_validation_suite.py`.
```

## What is currently verified

```{list-table}
:header-rows: 1

* - Property
  - Status
  - Anchor
* - `MassSegregationLayer` can be used through `generate_cluster_ic`
  - Unit-tested
  - `tests/unit/cluster/test_cluster_ic.py`
* - Energy-ordered assignment helper preserves shapes and mass ordering
  - Unit-tested
  - `tests/unit/cluster/test_mass_segregation.py`
* - Validation helper sweeps can compute Λ_MSR-style diagnostics
  - Unit-tested
  - `tests/unit/cluster/test_validation_suite.py`
* - Dedicated 10-realisation Λ_MSR table vs `lambda_seg`
  - Not currently pytest-anchored
  - Needs a future validation file or regenerated offline artifact
* - McLuster cross-code comparison
  - Not currently pytest-anchored
  - Planned/offline
```

## Spot values

Previously published spot values such as
`Λ_MSR = 1.92 ± 0.13` at `lambda_seg = 1.0` are best treated as
illustrative until they are regenerated by a committed validation
script and anchored in CI.

## How to run current checks

```bash
pytest tests/unit/cluster/test_cluster_ic.py -v
pytest tests/unit/cluster/test_mass_segregation.py -v
pytest tests/unit/cluster/test_validation_suite.py -v
```

## References

Theory at [](../10-theory/tidal-and-substructure/mass-segregation.md).
The physical motivation is {cite:t}`Baumgardt2008`; the alternative
interparticle-potential construction is {cite:t}`Subr2008`; the
diagnostic is {cite:t}`Allison2009`.
